# 4070 本地部署

部署日期：2026-09-06。仓库 `Yileina-Monv/BME`，分支 `main`，提交 `8ae3b51a6818eab41ff99a09fec5d96f85c74d61`。恢复附件：`retinal-migration-20260906-v1`。

**本机部署验收：PASS_LOCAL_DEPLOYMENT。** 20.22GB 附件全部通过分卷哈希校验，11,032 个项目文件恢复校验通过；冻结父版本、M1 辅助图、执行层和实际裁块均通过本机复验。

本机 RTX 4070 Laptop（8GB）已成功加载 M0、M1 的 10,000 步最终开发检查点，并分别完成一张 2048×2048 开发图的滑窗推理。没有启动新的研究训练。

## 直接运行

在 PowerShell 执行：

```powershell
Set-Location 'D:\Desktop\BME\AI\BME\AI临床应用'
& '.\.venv\Scripts\python.exe' '.\local_inference.py' --model m1
```

默认处理冻结开发集的第一张图。选择 M0：将 `--model m1` 改为 `--model m0`。选择其他开发图：增加 `--sample-index 1`（范围 0–117）。

输出目录会打印在终端，默认位于 `.runtime/local_inference/日期时间-模型/`，包含原图、血管二值图 `vessels.png`、绿色叠加图 `overlay.png`、原分辨率概率矩阵 `probability.npy` 和运行记录 `result.json`。每次使用新目录。

自己的图像：

```powershell
& '.\.venv\Scripts\python.exe' '.\local_inference.py' --model m1 --image 'D:\你的图片.png'
```

自动视野掩膜沿用 FIVES 的居中、正方形眼底图假设；其他构图应提供与原图同尺寸的二值视野掩膜：`--fov 'D:\视野掩膜.png'`。外部图像能够运行并不证明外部数据上的准确性。图像不缩小，使用 512 裁块、256 步长及高斯融合；默认推理 batch=2，可用 `--batch-size 1` 降低显存需求。

## 本机证据

| 检查 | 结果与记录 |
|---|---|
| Python、锁定依赖、CUDA、cuDNN | 与冻结运行环境完全一致；Python 3.13.11，PyTorch 2.12.1+cu130，torchvision 0.27.1+cu130 |
| CUDA 运算、FP16 前后向、BF16 推理、保存读取、双进程加载 | [本机兼容性验收](.runtime/deployment_4070/environment_compatibility.json) |
| M0 权重与整图推理 | [M0 运行记录](.runtime/deployment_4070/m0_smoke/result.json) |
| M1 权重与整图推理 | [M1 运行记录](.runtime/deployment_4070/m1_smoke/result.json)、[分割叠加图](.runtime/deployment_4070/m1_smoke/overlay.png) |
| 自定义输入与原冻结预处理一致性 | 同一图片、同一 FOV 的概率图逐像素完全一致；[记录](.runtime/deployment_4070/custom_input_verification.json) |
| 全量文件恢复 | [官方恢复器结果](MIGRATION_RESTORE_RESULT.json) |
| 冻结版本与实际裁块 | [本机最终验收](.runtime/deployment_4070/acceptance.json) |

单图检查使用开发图 `train/110_A.png`，仅因其所需附件先完成下载而选择，选择时没有查看模型成绩。M0/M1 各使用 49 个窗口，本次推理记录约 2.11/1.25 秒，PyTorch 峰值已分配显存均约 0.50GiB。计时受初始化、缓存和设备状态影响，不是速度排名或完整性能基准；显存值不是整机总占用。

M0 SHA-256：`8bce5b8f47c7fe43d553d4467adc46f5387eade014265b7a6a6f4b5211e67d79`。

M1 SHA-256：`74b2aab9afc7d14702508914f49deaf63b33b411671a6140a8c78c4dd8eafd56`。

## 4070 验收器差异

原始 `environment/verify_environment.py` 要求 GPU 架构逐字出现在编译列表中。锁定 PyTorch 包包含 `sm_86`，但未单列 `sm_89`，因此原脚本在 4070 上停止于这项检查；[原始失败记录](.runtime/deployment_4070/original_verifier_failure_4070.json) 已保留。

[NVIDIA 官方兼容规则](https://docs.nvidia.com/cuda/cuda-programming-guide/01-introduction/cuda-platform.html)说明，8.6 cubin 可在 8.9 GPU 上运行。本地新增 `local_deployment/verify_environment_4070.py`，仅将该前置条件改为同主版本、兼容次版本判断，并将报告与合成产物输出到独立目录；其余实际算子验收保留，已全部通过。原冻结脚本、锁定版本和历史 5080 验收记录保持原样。

复验本机算子可执行：

```powershell
& '.\.venv\Scripts\python.exe' '.\local_deployment\verify_environment_4070.py'
```

本机推理验收不代表已建立 4070/5080 全流程数值等价性，也不代表正式多种子、测试集或外部评价完成。旧训练票据仍绑定原机器；不要在 4070 上续跑旧任务。旧主登记仅恢复为迁移历史档案。

## 补充：真实训练配置短测（2026-09-06）

为判断训练适配程度，使用原 `retinal_prefetch.benchmark.gpu` 在独立诊断目录运行 M1 系数 0.1、512×512、microbatch=4、accumulation=1、FP16、两个预取线程。完成连续 64 步与 32+32 步恢复对照，总计 128 次工程优化器更新，没有签发研究任务或启动 C1。

**短程训练与同 GPU 断点恢复通过。** 连续／恢复终点的模型、优化器、AMP scaler、随机状态、历史和裁块指纹摘要全部一致；模型权重实际更新且模型／优化器张量均有限。旧 M0/M1 最终检查点和 Git 跟踪源码未改变。

- 连续 64 步：38.74 秒，平均 0.605 秒/步，预热后约 0.582 秒/步。
- 峰值 CUDA 已分配 3.32 GB、已保留 3.52 GB；采样整卡占用 5.55 GB。
- 进程峰值常驻 RAM 2.45 GB，私有提交 8.27 GB。GB 均为十进制；后者与物理常驻 RAM 不同。
- 按短测平均速度线性外推，10,000 步训练循环约 1.68 小时，仅供排期，不含额外验证、保存与重启，不能保证长时吞吐。
- 尚未验收 1–2 小时持续运行、跨 GPU 等价性或 C1 新算子的额外开销。

完整证据：[训练适配与恢复验收](.runtime/deployment_4070/training_readiness.json)。原 batch=4 已跑通，当前没有依据要求先改成 batch=2。冻结训练器本身只接受 batch=4、accumulation=1；如确需改变，须另行版本化并重做匹配验证。

Python 基础运行时位于 `D:\Desktop\BME\AI\.runtime\python-3.13.11\tools`，项目环境位于本目录 `.venv`。移动项目到另一台电脑时须重建环境。全量下载附件保存在上一级 `retinal-release-assets`，旧 `.venv` 作为附件档案保存，没有激活。


## 2026-09-06 训练提速验收

已验收两线程磁盘缓存：M1 训练约 2.47 倍速度，PAIR 约 2.23 倍速度，原训练状态及恢复结果一致。C1 小样本入口默认缓存，可用 `--loader original` 回退。缓存约 31.08 GB；正式研究尚未启动。详见[训练提速验收报告](D:/Desktop/BME/AI/BME/AI临床应用/训练提速验收报告.md)。
