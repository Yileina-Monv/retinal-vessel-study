# 眼底血管分割课程项目：实施规格

> 2026-09-06 状态说明：本文件保留 9 月 4 日原始方案规格。数据准备与 M0/M1 的 10,000 步开发比较已完成；M2−M1 及旧训练矩阵不再作为未经重新裁决的当前必做项。当前事实、创新候选和下一节点见[项目全局状态与研究决策](项目全局状态与研究决策.md)。本说明不修改任何已封装的代码、配置或数据。

- 版本日期：2026-09-04
- 项目周期：10 周相对日历窗口，精确起止日期待课程安排确认
- 小组规模：3 人
- 主数据：FIVES，取得与文件审计尚未完成
- 项目状态：**GO 主线实施；HOLD 患者独立性强主张、质量加权和 768 裁块；允许候选方法得到负结果**
- 配套研究报告：[`retinal_course_research_2026-09-04.md`](retinal_course_research_2026-09-04.md)

> 本文件是实施者可直接接手的规格，不代表数据、代码、训练或 GPU 测速已经完成。所有标为“待冻结”或“待实测”的字段，必须在对应关卡形成记录后才能进入正式实验。

---

## 1. 项目要回答什么

### 1.1 唯一主问题

在 FIVES 的固定图像级开发与测试协议下，相对于普通 Skeleton Recall Loss，在相同 2D U-Net、像素损失、裁块、训练预算和阈值规则下加入基于人工标签局部半径的粗细权重，能否提高**标签支持的细血管骨架召回**，同时不造成预先冻结的整图 Precision 不可接受下降？

### 1.2 主比较与辅助比较

- **主要比较：M2 − M1。** 回答粗细权重是否在普通 Skeleton Recall 之上提供额外价值。
- **辅助比较：M1 − M0。** 说明普通骨架监督相对于像素基线的效果，不作为粗细权重成败的替代结论。
- **可选比较：M3 − M2。** 仅在质量分支通过数据审计后运行；不据累计设计宣称质量与粗细存在交互作用。

### 1.3 允许的结论边界

正结果模板：

> 在本项目冻结的 FIVES 图像级协议、公开像素尺度和 2D U-Net 配置下，粗细加权 Skeleton Recall 相对于普通 Skeleton Recall 提高了标签支持的细血管骨架召回；整图 Precision 的变化处于预先冻结的可接受范围。该结果不等同于恢复真实解剖连续性、获得患者独立泛化或产生临床获益。

负结果模板：

> 在本项目测试的权重定义、损失系数、网络和训练预算下，粗细加权 Skeleton Recall 未显示相对于普通 Skeleton Recall 的稳定额外价值，或其召回收益伴随不可接受的 Precision 代价。该结果仅限制本次具体组合，不证明所有粗细加权方法无效。

禁止写入最终结论的表述：

- “恢复真实血管”或“恢复真实连续性”；
- “证明具有临床价值”；
- 在没有患者映射时写“患者独立”或“新患者泛化”；
- 将公开图像像素管径写成真实物理管径；
- 在非穷尽检索下写“首次提出”；
- 由统计未显著推导“两方法等价”或“绝对无效”。

---

## 2. 已确定、待核验与可删除项

| 项目 | 当前状态 | 实施决定 | 最迟冻结点 |
| --- | --- | --- | --- |
| 课程项目允许候选失败 | 已确定 | 保留负结果交付路线 | G0 |
| 主问题 | 推荐冻结 | 标签支持的细血管骨架召回，并以 Precision 为误检约束 | G0 |
| 基础网络 | 推荐默认 | 轻量 2D U-Net；正式组间完全一致 | G2 |
| 普通骨架对照 | 已裁决 | Skeleton Recall Loss | G0/G2 |
| 粗细定义 | 推荐默认 | 从整图人工标签的欧氏距离变换取得局部半径，以训练集骨架半径分位数定义“细” | G2 |
| 质量权重 | 降为可选 | 不进入 M0–M2；先保留质量分层评价 | G1b |
| FIVES 文件与字段 | 未核验 | 下载、哈希、图像—标签—质量表配对和像素值审计 | G1a |
| 患者/左右眼映射 | 未核实 | 可得则患者分组；不可得则诚实使用图像级协议 | 正式划分前 |
| 主指标数值参数 | 待冻结 | 训练集细血管阈值、验证集二值阈值、最小有意义改善、Precision 容许下降 | G2 |
| 主外部数据 | 推荐优先 | Fundus-AVSeg，作为明显域变化下的冻结测试 | G1c/G4 |
| 外部备选 | 推荐 | HRF；CHASE_DB1 只作可选压力测试 | G1c/G5 |
| 输入尺寸 | 初始默认 | 先用 512×512；768 只有在资源实测和收益证据支持时进入 | G2 |
| 正式预算 | 推荐冻结 | M0、M1、M2 各 3 个匹配随机种子，共 9 次完整训练 | G3 |
| 候选修订 | 有上限 | 最多一次有明确证据的问题驱动修订 | G3 |

---

## 3. 数据取得、清单与划分

### 3.1 FIVES 取得与证据状态

原始资料报告：FIVES 含 800 张 2048×2048 眼底图像，来自 573 名受试者，正常、糖尿病视网膜病变、年龄相关性黄斑变性和青光眼各 200 张；资料给出推荐的 600/200 划分、三项整图质量标签和 CC BY 4.0 许可。采集使用 50°、黄斑中心图像。标注流程明确不应仅为保持连续性而补画血管。

来源：

- [FIVES 原始数据论文](https://www.nature.com/articles/s41597-022-01564-3)
- [FIVES Figshare 数据记录](https://doi.org/10.6084/m9.figshare.19688169)

当前未完成：压缩包下载、文件哈希、逐文件配对、质量表实测、患者/左右眼映射和 FOV 文件核验。

### 3.2 最小数据清单

每张原图必须有一行清单。推荐 CSV 或 Parquet，字段如下：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `dataset` | 是 | 固定为 `FIVES` 或外部集名称 |
| `dataset_version` | 是 | 数据记录版本或下载日期 |
| `source_url` | 是 | 正式数据页或 DOI |
| `license` | 是 | 许可证及核验来源 |
| `archive_name` | 是 | 原始归档文件名 |
| `archive_sha256` | 是 | 原始归档哈希 |
| `image_id` | 是 | 数据集内唯一图像标识 |
| `image_path` | 是 | 原图相对路径 |
| `mask_path` | 是 | 总血管标签相对路径 |
| `fov_path` | 条件必填 | 官方 FOV 存在时填写；否则记录生成规则版本 |
| `image_sha256` | 是 | 图像哈希 |
| `mask_sha256` | 是 | 标签哈希 |
| `image_shape` | 是 | 高、宽、通道 |
| `mask_shape` | 是 | 高、宽 |
| `image_dtype` | 是 | 文件解码后的数据类型 |
| `mask_dtype` | 是 | 标签数据类型 |
| `mask_unique_values` | 是 | 实测唯一值列表 |
| `disease` | 是 | 正常、DR、AMD、青光眼等原始字段 |
| `quality_illumination_color` | 条件必填 | FIVES 质量表字段，保持原始编码 |
| `quality_blur` | 条件必填 | FIVES 质量表字段，保持原始编码 |
| `quality_low_contrast` | 条件必填 | FIVES 质量表字段，保持原始编码 |
| `patient_id` | 尽力取得 | 不可得时明确写 `UNAVAILABLE` |
| `eye` | 尽力取得 | 左、右或 `UNAVAILABLE` |
| `official_split` | 是 | 官方 Train/Test 或等价字段 |
| `project_split` | 是 | train/val/test/external |
| `provenance` | 是 | 原始、派生、转换脚本与版本 |
| `audit_status` | 是 | pending/pass/fail |
| `notes` | 否 | 异常、冲突与处置 |

### 3.3 训练前必须完成的数据检查

1. 记录下载来源、版本、归档大小、SHA-256 和许可证。
2. 解压后统计图像、标签、质量表和其他文件数量。
3. 使用 ID 规则配对，不依靠目录顺序。
4. 检查每个图像与标签尺寸一致，标签可二值化且没有意外灰度值。
5. 检查重复哈希、近重复图像和同一文件跨划分出现。
6. 核对质量表的工作表、列名、编码含义、重复 ID、缺失值和与图像的连接率。
7. 尽力寻找患者和左右眼字段；不得从文件名含义自行猜测。
8. 随机抽取至少 12 张图，并加入每种疾病和每种已出现质量问题的样例，叠加检查标签对齐。
9. 检查图像边缘、黑色背景与有效视野；冻结 FOV 生成或读取方式。
10. 生成审计报告、异常列表和可复现脚本，异常未解决的文件不得静默进入训练。

### 3.4 划分原则

优先级从高到低：

1. **患者级划分。** 若能核实匿名患者 ID，则同一患者所有眼图必须进入同一分区。
2. **官方图像级划分。** 若患者 ID 不可得，保留官方 600/200；从 600 张开发部分固定划出训练与验证，不改动官方 200 张测试。
3. 推荐图像级开发划分：600 张中 480 张训练、120 张验证。按疾病分层；质量字段只在核验后用于检查平衡，不把稀有组合机械塞入每一层。
4. 划分先于裁块。任何同一原图的裁块不得跨 train/val/test。
5. 保存固定 `split.csv`、生成脚本、随机种子和输入清单哈希。

患者映射不可得时，最终报告必须写：

> 内部开发和测试采用公开图像级协议；由于匿名患者与左右眼映射未能核实，不能排除同一患者不同眼图跨分区，也不作患者独立泛化主张。

### 3.5 FOV 规则

- 官方 FOV 掩膜存在时优先使用，并记录来源。
- 不存在时，使用只依赖输入图像的确定性规则生成；算法与参数在 G2 前冻结。
- 图像归一化统计、损失、阈值评价和指标均限制在 FOV 内。
- 不能通过模型预测或测试集成绩修改 FOV。
- FOV 边缘是否计入骨架指标必须由合成边缘案例验证并记录。

---

## 4. 预计算标签图

### 4.1 输入和输出

对每张完整标签图预计算，而不是先切块再计算：

- 二值标签；
- 单像素骨架；
- 标签内部欧氏距离变换；
- 膨胀后的骨架监督区域；
- 最近骨架点的局部半径传播图；
- 细血管骨架掩膜；
- FOV 掩膜。

设人工标签为：

$$
Y_n \in \{0,1\}^{H\times W}
$$

有效视野为：

$$
F_n \in \{0,1\}^{H\times W}
$$

骨架和距离变换为：

$$
S_n = \operatorname{skel}(Y_n \odot F_n)
$$

$$
D_n = \operatorname{EDT}(Y_n \odot F_n)
$$

骨架管状监督区域使用半径 2 像素的结构元素：

$$
M_n = \operatorname{dilate}(S_n, B_2) \odot Y_n \odot F_n
$$

### 4.2 局部粗细定义

骨架点上的局部半径定义为：

$$
r_n(x) = D_n(x), \quad x \in S_n
$$

对管状监督区域中的像素，使用最近骨架点的半径：

$$
\tilde r_n(x) = r_n\!\left(\operatorname{NN}_{S_n}(x)\right), \quad x \in M_n
$$

训练集参考半径：

$$
r_{\mathrm{ref}} = \operatorname{median}\{r_n(x): x\in S_n,\ n\in \mathcal D_{\mathrm{train}}\}
$$

仅使用训练集计算，随后保存为冻结元数据。这里的半径单位是公开图像像素，不是微米或真实物理管径。

### 4.3 粗细权重

推荐初始定义：

$$
a_n(x) = \operatorname{clip}\left[\left(\frac{r_{\mathrm{ref}}}{\tilde r_n(x)+\varepsilon}\right)^p, a_{\min}, a_{\max}\right]
$$

初始设计默认值：

$$
p=1, \qquad a_{\min}=0.5, \qquad a_{\max}=2.0, \qquad \varepsilon=10^{-6}
$$

这些值是待开发审计的设计默认，不是文献已验证的通用最优参数。G2 必须输出权重分布的最小值、分位数、最大值，以及不同疾病和粗细分组的覆盖情况。

### 4.4 细血管评价掩膜

从训练集所有标签骨架半径计算第一四分位数：

$$
\tau = Q_{0.25}\{D_n(x): x\in S_n,\ n\in \mathcal D_{\mathrm{train}}\}
$$

单图细血管骨架：

$$
S_n^{\mathrm{thin}} = \{x\in S_n : D_n(x)\leq \tau\}
$$

冻结后，验证、内部测试和各外部集不重新拟合该阈值。外部集可同时报告其自身半径分布作为描述，但不能用外部成绩反向修改主阈值。

### 4.5 缓存与版本

每个预计算输出必须与以下信息绑定：

- 原标签 SHA-256；
- FOV 规则版本；
- 骨架化库与版本；
- 距离变换库与版本；
- 结构元素定义；
- `r_ref`、`tau` 和计算数据范围；
- 预计算脚本 Git 提交；
- 输出数组形状、数据类型和哈希。

几何增强时，图像、标签、FOV、骨架、距离和权重图必须同步变换。初期只允许水平翻转、垂直翻转和 90° 整数倍旋转；不默认使用任意角度旋转、缩放或弹性变换。

---

## 5. 模型、输入与训练协议

### 5.1 2D U-Net 默认配置

这是简单、可复现、适合 8 GB 显存设备起步的设计默认，必须经过 G2 实测：

| 项目 | 初始值 | 状态 |
| --- | --- | --- |
| 输入 | RGB，512×512 裁块 | 推荐默认 |
| 输出 | 1 个 logit 通道 | 冻结 |
| 编码通道 | 32、64、128、256、512 | 待显存实测 |
| 降级通道 | 16、32、64、128、256 | 仅在 OOM 或速度不可接受时一次性切换 |
| 卷积 | 3×3，两次卷积/层 | 设计默认 |
| 归一化 | GroupNorm | 适应小微批量；组数需整除通道 |
| 激活 | ReLU | 设计默认 |
| 下采样 | 2×2 max pooling | 设计默认 |
| 上采样 | 双线性插值 + 卷积，或转置卷积二选一 | G2 前固定，不跨组改变 |
| Dropout | 0 | 保持基线简单 |
| 混合精度 | 启用 AMP | 待实测数值稳定性 |
| 微批量 | 1 起步 | 待显存实测 |
| 有效批量 | 4 起步，必要时梯度累积 | G2 冻结并跨组一致 |

若 32 通道起步模型在 512 裁块上 OOM 或吞吐明显影响 10 周交付，只允许在正式实验前整体降级一次；M0、M1、M2 必须使用同一最终网络。

### 5.2 输入归一化

- 只使用训练集、只在 FOV 内计算 RGB 通道均值和标准差。
- 保存统计量、训练清单哈希和计算脚本版本。
- 验证、测试和外部集沿用训练统计量，不对每个测试集单独拟合标准化参数。
- 可做每图有限强度裁剪，但规则必须在 G2 前冻结，不能根据外部成绩修改。

### 5.3 裁块采样

- 从完整图像同步裁取图像、标签和所有预计算图。
- 裁块中心位于 FOV 内；允许部分背景，但不以不同方法使用不同采样器。
- 不强制每个裁块有血管。无血管裁块仍用于像素损失，骨架损失置零并记录比例。
- 若前景过稀导致基线无法学习，可在 G2 前加入固定的前景/随机混合采样；必须对所有组一致，并记录抽样概率。
- 每次完整训练采用相同总优化步数，而不是依赖每个方法不同的“epoch”含义。

### 5.4 数据增强

主线默认：

- 水平翻转；
- 垂直翻转；
- 90° 整数倍旋转；
- 轻度亮度、对比度和色彩抖动，仅作用于图像；
- 不使用会改变标签几何但难以同步处理权重图的复杂变换。

所有概率和参数范围在 G2 前冻结。正式组间完全一致。

### 5.5 优化器与训练长度

可运行起点：

| 项目 | 初始设计默认 | 冻结规则 |
| --- | --- | --- |
| 优化器 | AdamW | 所有组一致 |
| 初始学习率 | `1e-3` | 允许仅根据 M0/M1 开发曲线在 G2 调整一次 |
| 权重衰减 | `1e-4` | G2 冻结 |
| 学习率策略 | cosine decay | G2 冻结 |
| 梯度裁剪 | 全局范数 1.0 | 仅作为数值保护，记录触发率 |
| 最大步数 | `FREEZE_AT_G2` | 依据 M0 收敛曲线和时间实测确定 |
| 验证间隔 | 总步数约 20 次验证 | 冻结为整数步数 |
| 早停 | 不用于组间不同长度训练 | 正式组均运行冻结预算；最佳检查点由同一验证规则选择 |

不要用 M2 的正式结果调学习率、训练长度或增强。若需一次候选修订，必须先记录明确失败机制并重新声明探索/正式边界。

### 5.6 检查点选择

- 固定以验证集整图 Dice 选择检查点，避免用主指标同时选模型、选阈值和评价候选造成过度适配。
- 每次训练保存最后检查点和最佳验证 Dice 检查点。
- 所有组使用同一选择规则和验证频率。
- 测试集和外部集不得参与检查点选择。

---

## 6. 损失函数与实现顺序

### 6.1 预测

设网络输出为：

$$
Z_n = f_{\theta}(X_n)
$$

概率为：

$$
P_n = \sigma(Z_n)
$$

### 6.2 像素基线

主线像素损失：

$$
\mathcal L_{\mathrm{pix}} = \mathcal L_{\mathrm{BCE}} + \mathcal L_{\mathrm{Dice}}
$$

BCE 和 soft Dice 只在 FOV 内计算。Dice 分子和分母都加入同一个数值平滑项，具体值在单元测试中固定。

### 6.3 普通 Skeleton Recall Loss

普通骨架召回项：

$$
\mathcal L_{\mathrm{SRec},n} = 1 - \frac{\sum_x M_n(x)P_n(x)+\varepsilon}{\sum_x M_n(x)+\varepsilon}
$$

M1：

$$
\mathcal L_{M1} = \mathcal L_{\mathrm{pix}} + \lambda_s \mathcal L_{\mathrm{SRec}}
$$

原方法作者实验过的连接系数包含 0.1 和 1.0。实施时只在开发阶段比较这两个候选或更小的单次网格，然后冻结一个值；M1 和 M2 必须共享同一个最终系数。

### 6.4 粗细加权 Skeleton Recall Loss

粗细加权项：

$$
\mathcal L_{\mathrm{WSRec},n} = 1 - \frac{\sum_x M_n(x)a_n(x)P_n(x)+\varepsilon}{\sum_x M_n(x)a_n(x)+\varepsilon}
$$

M2：

$$
\mathcal L_{M2} = \mathcal L_{\mathrm{pix}} + \lambda_s \mathcal L_{\mathrm{WSRec}}
$$

该加权版本是本项目的设计组合，不继承 clDice 或普通 Skeleton Recall 的所有理论性质，也不应包装为已被文献验证的改进。

### 6.5 空骨架和零值处理

- 当裁块内 `sum(M)==0` 时，该裁块的骨架项定义为 0，像素损失照常计算。
- 批次聚合时按有效骨架裁块平均；同时记录空骨架裁块比例。
- 不将空骨架裁块的 `1 - epsilon/epsilon` 当作有效监督。
- 距离变换只在标签前景和 FOV 内使用；背景权重不参与骨架项。
- 检查 `a` 是否有限，任何 NaN、Inf 或超界值立即终止该运行。
- AMP 下损失应以 FP32 累加分子和分母，避免小骨架区域精度损失。

### 6.6 为什么质量系数不能放在逐图归一化分子和分母中

若对单图使用标量质量系数：

$$
1-\frac{q_n\sum_x M_n(x)P_n(x)}{q_n\sum_x M_n(x)}
$$

则在非零情况下质量系数会抵消，不能改变该图的相对贡献。可选质量分支必须将系数作用在已归一化的逐图损失外侧。

### 6.7 可选质量分支

只有 G1b 通过时定义：

$$
z_n = 1 - \frac{1}{3}\sum_{j=1}^{3}s_{nj}
$$

$$
q_n^{\mathrm{raw}} = 1 + \gamma z_n
$$

$$
q_n = \frac{q_n^{\mathrm{raw}}}{\operatorname{mean}_{m\in\mathcal D_{\mathrm{train}}} q_m^{\mathrm{raw}}}
$$

初始探索值：

$$
\gamma=0.5
$$

M3：

$$
\mathcal L_{M3} = \mathcal L_{\mathrm{pix}} + \lambda_s q_n \mathcal L_{\mathrm{WSRec},n}
$$

该分支只可在以下条件同时满足时进入：

1. 质量表与图像完全配对；
2. 低质量样本数量足以形成有意义的开发分层；
3. 质量与疾病分布没有严重到无法解释的混杂，或报告明确按疾病分层；
4. 盲法样例复核没有显示低质量图标签系统性不可用；
5. M0–M2 主线不因加入 M3 延迟收尾。

若不满足，保留质量分层结果，不运行 M3。

### 6.8 实现伪代码

```python
# Full-image preprocessing, once per label/FOV version.
y = load_binary_mask(mask_path)
fov = load_or_build_fov(image_path)
y_valid = y & fov
skeleton = skeletonize(y_valid)
distance = edt(y_valid)
tube = dilate(skeleton, radius=2) & y_valid
radius_map = nearest_skeleton_radius(distance, skeleton, tube)
weight_map = clip((r_ref / (radius_map + eps)) ** p, a_min, a_max)
save_maps(skeleton, distance, tube, radius_map, weight_map, fov)

# Training step.
image, y, fov, tube, weight = sample_synchronized_patch(...)
logits = model(image)
prob = sigmoid(logits)
pixel_loss = bce_dice_loss(logits, y, fov)

if tube.sum() == 0:
    skeleton_loss = 0.0
else:
    numerator = sum_fp32(prob * tube * weight)
    denominator = sum_fp32(tube * weight)
    skeleton_loss = 1.0 - (numerator + eps) / (denominator + eps)

loss = pixel_loss + lambda_s * skeleton_loss
loss.backward()
optimizer.step()
```

M1 将 `weight` 固定为 1；M0 将骨架项固定为 0。

---

## 7. 滑窗整图推理

### 7.1 默认规则

- 输入窗口：512×512；
- 重叠比例：0.5；
- 融合：Gaussian 权重；
- 窗口不足时使用对称填充，并在输出恢复到原始尺寸；
- 最终概率图乘以 FOV；
- 保存未阈值化概率图和二值图。

可使用 MONAI `sliding_window_inference` 或等价自实现，但必须锁定版本和参数。参考：[MONAI sliding window inference 文档](https://docs.monai.io/en/stable/inferers.html)。

### 7.2 拼接单元测试

至少验证：

1. 恒定输入经过“直接整图网络”和“滑窗网络”输出一致到预定容差；
2. 单点、水平线、垂直线和跨窗口斜线没有固定网格断裂；
3. 重叠变化不会造成明显概率台阶；
4. 图像小于窗口时尺寸恢复正确；
5. FOV 外输出被清零，但 FOV 边缘不被重复裁掉；
6. 批量推理和单图推理结果一致；
7. 推理显存峰值和每图时间被记录。

若出现接缝，先修复推理，再进行正式比较；接缝错误不能被当作方法负结果。

---

## 8. 指标定义与计算验证

### 8.1 二值化

验证集上选择阈值：

$$
\widehat Y_{n,t} = \mathbf 1[P_n\geq t]\odot F_n
$$

阈值选择规则：在预先给定网格上最大化细血管骨架召回，同时满足冻结的 Precision 下限；并列时选择 Dice 更高者。阈值由验证集确定，随后对内部测试和外部数据冻结。

### 8.2 主指标：细血管骨架召回

距离容差为：

$$
R_{\mathrm{thin},n}(\delta) = \frac{\sum_{x\in S_n^{\mathrm{thin}}}\mathbf 1[d(x,\widehat Y_{n,t})\leq\delta]}{|S_n^{\mathrm{thin}}|}
$$

主报告使用：

$$
\delta=1
$$

敏感性分析使用：

$$
\delta\in\{0,2\}
$$

规则：

- 使用完整图像、原始公开像素尺度；
- 以标签细血管骨架到预测前景的距离计算；
- 主分析不删除短分支；若审计发现孤立标注噪声，可另作预先定义的敏感性分析；
- 标签细血管骨架为空时记为 NA，不记 0 或 1，并报告图像数量；
- 外部集仍使用 FIVES 训练集冻结的阈值 `tau`，同时说明像素尺度不可物理比较。

### 8.3 次要指标

| 指标 | 作用 | 不能单独证明什么 |
| --- | --- | --- |
| hard clDice | 双向骨架—区域覆盖，观察结构一致性 | 不能证明细血管改善，也可能漏掉错误桥接 |
| Dice | 总体像素重叠 | 粗血管占比高时可能掩盖细血管变化 |
| Precision | 约束误检与过度连接 | 不能判断所有预测连接的解剖正确性 |
| Recall/Sensitivity | 总体标签前景覆盖 | 不区分粗细血管 |
| AUPRC | 避免只看单一阈值 | 仍受标签质量和前景比例影响 |

不把端点数量直接命名为“真实断裂数”。连通分量、端点和分支数量只可作为机制描述或案例抽样依据。

### 8.4 合成指标测试

用简单二值图生成以下扰动，并记录每个指标的预期与实际响应：

| 扰动 | 预期检查 |
| --- | --- |
| 删除线段形成间隙 | 细血管召回下降；clDice 应下降；Precision 可能不变 |
| 添加错误桥接 | Precision 下降；细血管召回可能不下降；用于证明召回不是充分条件 |
| 整体增粗 | Dice 和 Precision 变化；clDice 可能过于宽容 |
| 平移 1 像素 | 验证距离容差 0、1、2 的敏感性 |
| 跨滑窗边界断开 | 检查拼接错误是否被指标捕获 |
| 空标签与空预测 | 验证 NA、平滑项和日志规则 |
| 短孤立分支 | 检查不剪枝与敏感性剪枝的差异 |
| 交叉处错误连边 | 检查自动指标的盲点，并保留人工案例复核 |

这类测试只验证计算行为，不替代真实数据有效性证据。

### 8.5 人工案例复核

正式解盲前冻结案例规则：

- 基础样本：每种疾病 4 张，共 16 张，由固定随机种子抽取；
- 机制样本：根据不含组名的指标差值选择 8 张极端案例，覆盖提升与恶化；
- 评审图隐藏方法名，显示原图、人工标签、预测叠加和局部放大；
- 记录“标签支持的漏检”“可能错误桥接”“低可见区域”“标签本身断开”“无法判断”；
- 不将学生评审包装为专家金标准；
- 所有失败案例都保留，不只展示漂亮结果。

---

## 9. 冻结、统计与结果解释

### 9.1 G2 前必须冻结的项目

- 数据版本、清单和划分；
- FOV 规则；
- 网络宽度、输入尺寸和有效批量；
- 优化器、学习率、步数、验证频率和检查点选择；
- 增强与裁块采样；
- `lambda_s`、权重公式和上限；
- `tau`、距离容差和骨架化实现；
- 阈值选择规则；
- 最小有意义改善；
- Precision 容许下降；
- 主比较 M2−M1；
- 随机种子；
- 异常运行排除规则；
- 外部数据二值化与 FOV 规则。

### 9.2 数值门槛如何确定

不能在没有开发结果时编造通用百分点门槛。G2 使用以下证据，只看 M0/M1 开发结果：

1. 三个短重复或至少两个不同种子的开发波动；
2. 指标单元测试的量化分辨率；
3. 验证集逐图差值分布；
4. 盲法叠加图中可见差异；
5. 课程所能解释的实际效果大小。

据此冻结：

- `delta_R_thin_min`：最小有意义细血管召回改善；
- `delta_P_max`：可容许 Precision 下降上限。

冻结记录必须带日期、参与人、输入文件哈希和理由。不得查看 M2 内部测试或外部成绩后修改。

### 9.3 正式统计单位

1. 每个模型先生成每张整图的所有指标。
2. 同一方法三个随机种子先在图像内平均，形成该方法的逐图估计；同时单独报告种子间标准差和每个种子结果。
3. 主要比较计算同一图像上的配对差值。
4. 患者 ID 可得时，使用患者聚类 bootstrap；同一患者两眼作为同一簇。
5. 患者 ID 不可得时，使用图像级配对 bootstrap，并明确它不能估计患者采样不确定性。
6. 推荐报告平均差或中位差、95% bootstrap 区间、逐图分布和三个种子结果；显著性检验不是唯一判据。
7. 不把裁块、左右眼或随机种子当新增独立患者。

### 9.4 结果分类与行动

| 结果 | 判定条件 | 行动 | 报告口径 |
| --- | --- | --- | --- |
| 稳定正结果 | M2−M1 达到冻结的最小改善，Precision 代价在上限内，种子方向一致 | 冻结主结论；执行主外部测试 | 局限于标签支持、公开像素尺度和本协议 |
| 无明显额外价值 | 差值小、区间宽或跨种子方向不稳，且实现检查通过 | 不继续堆模块；完成外部/案例与负结果报告 | “未显示稳定额外价值”，不写等价 |
| Precision 代价过大 | 召回提高但超过冻结误检上限 | 判为不值得采用；分析错误桥接和增粗 | 说明权衡，不只报召回 |
| 跨种子冲突 | 个别种子大幅提升，其他无效或恶化 | 检查训练日志；若无实现错误，作为不稳定结果 | 报完整种子结果，不挑最佳种子 |
| 内部有效、外部不支持 | 内部主比较通过，外部差值弱或反向 | 不适配外部测试；分析域和标签规则差异 | 内部条件有效，外部可迁移证据不足 |
| 实现/评价错误 | 错位、泄漏、指标错误、滑窗接缝、异常数值 | 修复并重跑；不能计作有效负结果 | 记录失败运行与修复 |
| 外部不可得 | 下载、许可或格式无法在期限内解决 | 启用 HRF；仍不可得则内部降级交付 | 明确未完成外部验证，不虚构结论 |

### 9.5 一次候选修订预算

只允许一次问题驱动修订，示例：

- 权重饱和导致大量像素取 `a_max`：仅调整 `p` 或上限；
- 骨架项尺度压过像素项：仅调整 `lambda_s`；
- 预计算管状区域与标签边界错位：修复实现，不算方法修订；
- 512 裁块上下文不足且资源允许：可评估 768，但这会改变所有组，且必须在正式冻结前完成。

修订必须形成一页记录：现象、证据、唯一改动、验收标准、重试上限和是否重新开始正式阶段。没有明确机制时停止修改，进入负结果交付。

---

## 10. 最小实验表

### 10.1 正式组

| 组 | 网络与像素损失 | 骨架项 | 回答的问题 | 随机种子 |
| --- | --- | --- | --- | --- |
| M0 | 固定 2D U-Net + BCE + soft Dice | 无 | 可信像素基线能达到什么水平 | 2026、2027、2028 |
| M1 | 与 M0 完全相同 | 普通 Skeleton Recall | 普通标签骨架监督相对像素基线是否有价值 | 2026、2027、2028 |
| M2 | 与 M0 完全相同 | 粗细加权 Skeleton Recall | 粗细权重相对普通 Skeleton Recall 是否提供额外价值 | 2026、2027、2028 |
| M3 | 与 M0 完全相同 | 粗细加权 Skeleton Recall，再乘整图质量系数 | 仅在 G1b 通过后探索质量加权 | 可选，默认不运行 |

正式最小预算为 9 次完整训练。M0 合规运行可复用，但必须满足相同代码版本、数据划分、训练预算、检查点规则和日志要求。

### 10.2 每组调参公平性

- 网络、输入尺寸、采样、增强、优化器、训练步数和检查点选择全部共享。
- `lambda_s` 只在正式实验前共同选择；M1/M2 不分别搜索更有利值。
- M2 的 `p` 和上下限只允许一次开发阶段选择，搜索预算必须记录。
- 不能用内部测试或外部成绩挑权重、阈值或种子。
- 不按每组“最佳种子”比较；所有预定种子全部报告。

### 10.3 短跑与完整训练

短跑只用于：

- 发现 OOM、NaN、数据错位和吞吐问题；
- 确认损失下降和梯度非零；
- 比较少量 `lambda_s` 候选；
- 冻结完整步数和时间预算。

短跑未收敛时，不能凭微小指标差异宣布候选胜负。正式结果只来自冻结配置的完整训练。

### 10.4 每次运行必须保存

```text
run_id/
  config_resolved.yaml
  environment.txt
  git_commit.txt
  data_manifest_hash.txt
  split_hash.txt
  precompute_manifest_hash.txt
  seed.txt
  train.log
  metrics_step.csv
  resource_profile.json
  checkpoints/
    best_val_dice.pt
    last.pt
  val_predictions/
  test_predictions/
  per_image_metrics.csv
  failure_record.md
```

`resource_profile.json` 至少包含：设备名称、驱动、CUDA、框架版本、峰值显存、微批量、累积步数、每步中位时间、完整训练时长、推理时长和异常。

---

## 11. 外部验证实施

### 11.1 主外部候选：Fundus-AVSeg

定位：**不同医院、设备、图像尺寸与标注体系下的冻结域变化测试**，不是已经证实的近似条件患者测试。

原始资料报告：100 张图来自 100 名患者，正常 40 张，DR、AMD 和青光眼各 20 张；公开两种图像尺寸；标签以五类颜色表达动脉、静脉、交叉、类别不明血管和背景；数据记录为 CC BY 4.0。来源：

- [Fundus-AVSeg 原始数据论文](https://www.nature.com/articles/s41597-025-05381-2)
- [Fundus-AVSeg Figshare](https://doi.org/10.6084/m9.figshare.27938034)

正式使用前必须：

1. 记录版本、许可、归档哈希和文件数量。
2. 实测每个标签的 RGB 唯一值和异常值。
3. 在文件审计确认后，将动脉、静脉、交叉和类别不明血管并为总血管前景；黑色背景为 0。
4. 检查标签是否有抗锯齿、透明通道、压缩伪影或未说明颜色。
5. 核对图像 ID、患者字段、尺寸和质量字段。
6. 冻结 FOV 与缩放规则。优先在公开原始尺寸进行滑窗推理，不把图像缩到统一 512×512 整图。
7. 使用 FIVES 冻结模型、归一化统计和二值阈值；不训练、不适配、不根据成绩挑子集。
8. 报告完整外部集结果；可按其原生质量字段分层，但不强行映射为 FIVES 三项质量标签。

### 11.2 备选：HRF

原始机构页报告 45 张分割图，健康、DR 和青光眼各 15 张，提供二值分割标签及部分 FOV 掩膜，CC BY 4.0。质量评估数据是另一套 18 对图像，不得混入分割集统计。来源：[HRF 官方数据页](https://www5.cs.fau.de/research/data/fundus-images/)。

启用条件：Fundus-AVSeg 在截止前无法取得、许可/文件格式无法裁决，或主外部审计失败。HRF 同样只作冻结测试。

### 11.3 可选压力测试：CHASE_DB1

官方记录报告 28 张图来自 14 名儿童双眼，每图有两套观察者标签，使用手持相机和 30° 视野，CC BY 4.0。来源：[CHASE_DB1 官方记录](https://researchinnovation.kingston.ac.uk/en/datasets/chasedb1-retinal-vessel-reference-dataset-4/)。

仅在主线完成且有余量时运行：

- 以儿童为统计簇；
- 预先固定观察者 1 为主参考，观察者 2 为敏感性分析，或按实际文件命名作相反固定；
- 不选择得分更高的观察者作为主结果；
- 明确它是人群与采集条件变化更大的压力测试；
- CHASE-AV 是同一图像的派生标签，不计作新患者来源。

### 11.4 外部失败降级

外部数据都不可得时，课程项目仍可完成：

- 提供取得尝试、许可判断和失败记录；
- 完成 FIVES 冻结测试、疾病/质量分层和失败案例；
- 将外部验证列为未完成限制；
- 不以合成退化替代真实外部患者证据；
- 保留可在数据取得后直接运行的外部推理脚本与标签适配单元测试。

---

## 12. G0–G5 关卡

### G0：研究问题与交付边界

| 项目 | 内容 |
| --- | --- |
| 输入证据 | 本研究报告、课程项目允许失败、10 周窗口、三人小组条件 |
| 通过条件 | 主问题、M2−M1 主比较、允许结论和负结果模板获得小组确认 |
| 未通过行动 | 缩为 M0−M1 骨架监督适用性项目，不停止课程项目 |
| 可并行任务 | 数据下载、仓库骨架、文献卡片 |
| 人工裁决 | 三人都能用自己的话解释主问题、误检约束和主张边界 |

### G1a：训练数据可用

| 项目 | 内容 |
| --- | --- |
| 输入证据 | FIVES 原始归档、质量表、许可证、数据清单 |
| 通过条件 | 图像—标签配对完整；像素值和尺寸合法；划分冻结；无未处理的泄漏或错位 |
| 未通过行动 | 修复清单或剔除明确损坏文件并记录；仍不可用则转为其他公开二值血管集并重写主张 |
| 可并行任务 | 损失单元测试、合成指标测试、训练环境搭建 |
| 人工裁决 | 异常文件处置、患者映射证据是否充分 |

### G1b：质量分支

| 项目 | 内容 |
| --- | --- |
| 输入证据 | 质量表实测、质量×疾病交叉表、盲法标签可用性复核 |
| 通过条件 | 配对完整、样本量可解释、标签在低质量样例中仍可用、主线不被拖延 |
| 未通过行动 | 删除 M3；仅作质量分层描述 |
| 可并行任务 | M0–M2 实施不受影响 |
| 人工裁决 | 是否值得为 M3 增加 3 次训练 |

### G1c：外部证据

| 项目 | 内容 |
| --- | --- |
| 输入证据 | Fundus-AVSeg 或 HRF 的归档、许可、标签和文件审计 |
| 通过条件 | 取得合法；总血管二值化规则唯一；FOV/尺寸处理冻结；无训练适配 |
| 未通过行动 | Fundus-AVSeg → HRF → 内部降级交付 |
| 可并行任务 | 内部训练与报告框架 |
| 人工裁决 | 标签合并和冲突来源处理 |

### G2：基础流程可用、测量可信、资源实测完成

| 项目 | 内容 |
| --- | --- |
| 输入证据 | M0 短跑、完整整图推理、指标单元测试、显存与速度记录 |
| 通过条件 | 无错位/泄漏/接缝；M0 可学习；指标响应符合合成预期；512 配置能在可用设备运行；冻结协议形成 |
| 未通过行动 | 修复基础流程；必要时降级网络宽度；仍失败则缩为可复现基线与指标研究 |
| 可并行任务 | 外部数据审计、文献与报告表格 |
| 人工裁决 | 最终网络宽度、训练步数、数值门槛 |

### G3：候选继续、一次修订或停止修改

| 项目 | 内容 |
| --- | --- |
| 输入证据 | M1/M2 短跑曲线、权重分布、梯度与案例检查 |
| 通过条件 | 实现正确、M2 没有明显数值病态，正式预算可承受 |
| 未通过行动 | 一次有依据修订；仍不通过则停止修改，正式完成 M0–M1 或可解释 M2 失败 |
| 可并行任务 | 基线复用审计、结果脚本、演示模板 |
| 人工裁决 | 是否使用唯一修订预算；G3 不决定课程项目生死 |

### G4：冻结实验支持多强的结论

| 项目 | 内容 |
| --- | --- |
| 输入证据 | 9 次正式训练、逐图指标、bootstrap、失败案例、外部结果或失败记录 |
| 通过条件 | 所有预定运行或有据排除；主比较完整；负结果同样可解释；限制清楚 |
| 未通过行动 | 降低结论强度，补必要的实现核验，不追加无依据模型 |
| 可并行任务 | 复现、报告和演示 |
| 人工裁决 | 正/负/不确定结论类别与用词 |

### G5：是否增加扩展

| 项目 | 内容 |
| --- | --- |
| 输入证据 | 主线、复现和报告均已完成，仍有时间与 GPU 余量 |
| 通过条件 | 扩展回答一个明确问题，且不危及交付 |
| 可选扩展 | M3、CHASE_DB1、第二外部集、预定义形态误差分析中的一个 |
| 未通过行动 | 不扩展，直接收尾 |
| 人工裁决 | 教学价值是否超过新增复杂度 |

---

## 13. 三人职责与后台任务包

### 13.1 角色

| 角色 | 主责 | 必须理解与复核 |
| --- | --- | --- |
| A：模型与实验 | U-Net、损失、训练入口、资源实测、运行登记 | 数据对齐、主指标、失败机制、所有组公平性 |
| B：数据与评价 | 清单、FOV、预计算图、指标、统计、外部标签转换 | 损失所用标签图、阈值冻结、患者/图像统计单位 |
| C：证据与交付 | 原始来源、版本/许可、案例复核、报告和演示 | 能追溯每个结论，运行一次独立复现，不只整理文字 |

自动化完成不等于学生掌握。每个关卡至少有一名非主开发者复核关键产物。

### 13.2 轻量交接记录

每个任务包提交一个 Markdown：

```text
# Task handoff
- Task ID:
- Owner:
- Input versions and hashes:
- Commands executed:
- Outputs:
- Acceptance checks:
- Known failures or uncertainties:
- Decision required:
- Reviewer and review date:
```

### 13.3 后台任务包

#### P1：数据取得与审计

- 目标：形成可训练 FIVES 清单和异常报告。
- 输入：正式数据页、原始归档、质量表。
- 产物：`manifest.csv`、`split.csv`、哈希清单、质量交叉表、叠加样例、`data_audit.md`。
- 验收：B 主做，A 随机复核至少 12 张，C 核验来源与许可。
- 依赖：下载成功。
- 重试上限：下载/解压基础设施错误每种方式 1 次；数据异常不靠重复下载掩盖。
- 交接点：G1a、G1b。

#### P2：损失与指标

- 目标：实现 M0–M2 损失、预计算图、滑窗和指标。
- 输入：少量经审计样例、公式、固定库版本。
- 产物：单元测试、合成测试结果、可视化、最小训练冒烟测试。
- 验收：A 主做，B 逐项核对公式和边界情形，C 能运行测试。
- 依赖：不需完整数据，可先用合成图。
- 修订上限：实现错误不限于一次方法修订；必须修复并记录。
- 交接点：G2。

#### P3：基线与资源实测

- 目标：确认 512 配置、训练长度、显存和吞吐。
- 输入：冻结开发划分、M0、设备。
- 产物：M0 曲线、整图预测、资源报告、最终基础配置。
- 验收：损失下降、整图结果合理、无接缝、峰值显存有余量。
- 依赖：G1a、P2。
- 重试上限：同配置基础设施失败重跑 1 次；OOM 时仅一次网络降级。
- 交接点：G2。

#### P4：候选筛选与冻结

- 目标：选择 `lambda_s`，审计粗细权重，决定是否使用唯一修订。
- 输入：M0/M1 开发结果、M2 短跑、权重分布。
- 产物：`freeze_protocol.md`、参数文件、决策记录。
- 验收：未查看测试结果；参数变更有明确证据；三人签字。
- 依赖：G2。
- 修订上限：方法定义最多 1 次有依据修订。
- 交接点：G3。

#### P5：正式训练与冻结评测

- 目标：完成 9 次训练、内部测试和一个主外部测试。
- 输入：冻结配置和代码提交。
- 产物：全部运行目录、逐图指标、统计、案例包、外部结果或失败记录。
- 验收：运行矩阵完整；无测试反馈调参；所有种子报告。
- 依赖：G3、G1c。
- 重试上限：同配置基础设施失败可重跑 1 次；方法失败不重复刷种子。
- 交接点：G4。

#### P6：复现与交付

- 目标：从原始数据说明和固定权重重建主要结果。
- 输入：代码、环境、数据清单、配置、检查点和结果脚本。
- 产物：复现日志、最终表格、报告、演示、推理命令。
- 验收：至少一名非主开发组员在独立环境完成主要结果重建；差异在预定容差内。
- 依赖：P5。
- 修订上限：只修复交付错误，不追加候选模型。
- 交接点：课程提交。

---

## 14. 资源与时间模型

### 14.1 GPU 时间

分别记录每种方法的实测完整训练时间：

$$
T_0,\ T_1,\ T_2,\ T_3
$$

正式主线 GPU 时间：

$$
G_{\mathrm{formal}} = 3T_0 + 3T_1 + 3T_2
$$

若质量分支进入：

$$
G_{\mathrm{formal+quality}} = G_{\mathrm{formal}} + 3T_3
$$

另计短跑、失败重跑、整图推理和外部测试。不能仅根据显卡型号承诺速度。

### 14.2 主动人工负担

不再用旧版 200–300 人时直接判定 10 周不足。按产物与复核点估计：

| 类别 | 主动人工内容 | 负担特征 |
| --- | --- | --- |
| 数据 | 来源/许可、异常处置、叠加复核、划分决定 | 早期集中，无法完全自动化 |
| 实现 | 损失、指标、滑窗与单元测试 | 技术核心，需要两人交叉复核 |
| 运行 | 启动、异常判断、冻结记录 | 不需全程盯训练，但每次运行要验收 |
| 解释 | 统计、失败案例、结论边界 | 不能交给后台产物自动定论 |
| 复现 | 独立运行和结果对照 | 课程学习目标，必须保留人工参与 |

### 14.3 后台产物数量

主线预计形成少量稳定产物，而非大型平台：

- 1 套 FIVES 数据清单与审计；
- 1 套预计算标签图和清单；
- 1 套损失/指标/滑窗测试；
- 1 份冻结协议；
- 9 个正式训练目录；
- 1 套内部统计与案例包；
- 1 套主外部结果或失败记录；
- 1 份独立复现记录；
- 研究报告、实施规格和课程最终报告。

### 14.4 相对阶段安排

| 阶段 | 主要目标 | 必须留出的收尾空间 |
| --- | --- | --- |
| 前段 | G0、数据取得、P2 合成测试、G1a/G1b/G1c | 不等待所有外部证据才写基础代码 |
| 中前段 | G2 基线、资源实测、冻结训练协议 | 及时降级网络，避免后期才发现 OOM |
| 中段 | M1/M2 筛选、唯一修订、G3 | 停止无依据加模块 |
| 中后段 | 9 次正式训练、内部冻结测试、主外部测试 | 每次运行即生成可追溯产物 |
| 后段 | G4、案例、复现、报告和演示 | 主线结束后才考虑 G5 扩展 |

收尾条件：当主线训练、统计、失败记录、可运行推理和独立复现均可交付时，停止新增方法。任何扩展不得挤占最终报告和演示。

---

## 15. 代码与配置最小结构

```text
project/
  README.md
  LICENSES.md
  environment.yaml
  configs/
    data_fives.yaml
    model_unet.yaml
    train_base.yaml
    m0_pixel.yaml
    m1_srec.yaml
    m2_weighted_srec.yaml
    m3_quality_optional.yaml
    freeze_protocol.yaml
  data_manifests/
    fives_manifest.csv
    split.csv
    external_manifest.csv
  src/
    data.py
    fov.py
    precompute.py
    model.py
    losses.py
    sliding_inference.py
    metrics.py
    train.py
    evaluate.py
    statistics.py
  tests/
    test_pairing.py
    test_precompute.py
    test_losses.py
    test_metrics_synthetic.py
    test_sliding_window.py
  scripts/
    audit_fives.py
    build_precomputed_maps.py
    run_matrix.py
    audit_fundus_avseg.py
    reproduce_main_results.py
  reports/
    data_audit.md
    freeze_protocol.md
    failure_log.md
    result_tables.md
    reproduction.md
  runs/
```

不要为三人课程项目建设数据库、任务编排平台或复杂插件系统。一个可复现命令行入口、清晰配置和运行清单足够。

### 15.1 配置骨架

```yaml
project:
  name: retinal_course
  seed_list: [2026, 2027, 2028]

data:
  dataset: FIVES
  manifest: data_manifests/fives_manifest.csv
  split: data_manifests/split.csv
  patch_size: 512
  fov_rule: FREEZE_AT_G1A
  normalization: train_fov_channel_mean_std

model:
  name: unet2d
  in_channels: 3
  out_channels: 1
  channels: [32, 64, 128, 256, 512]
  normalization: groupnorm
  activation: relu

training:
  optimizer: adamw
  learning_rate: 1.0e-3
  weight_decay: 1.0e-4
  scheduler: cosine
  micro_batch: 1
  effective_batch: 4
  max_steps: FREEZE_AT_G2
  amp: true
  checkpoint_metric: val_dice

loss:
  pixel: bce_plus_soft_dice
  skeleton_mode: none
  lambda_s: FREEZE_AT_G2
  tube_radius_px: 2
  p: 1.0
  a_min: 0.5
  a_max: 2.0
  eps: 1.0e-6

inference:
  window_size: 512
  overlap: 0.5
  blend: gaussian
  threshold_rule: FREEZE_AT_G2

metrics:
  primary: thin_skeleton_recall
  thin_radius_quantile: 0.25
  distance_tolerance_px: 1
  sensitivity_tolerances_px: [0, 2]
  secondary: [cldice, dice, precision, recall, auprc]
```

每个正式运行必须保存解析后的完整配置，不只保存差异项。

---

## 16. 最少必要的单元与集成检查

### 16.1 数据

- 图像和标签 ID 一一对应；
- 形状、方向和 FOV 对齐；
- 标签只有允许值；
- 质量表连接率为 100%，否则异常逐条处置；
- 划分前后图像哈希无交叉；
- 患者 ID 可得时患者无交叉；
- 裁块不跨原图分区。

### 16.2 损失

- 完美预测时像素损失和骨架损失接近 0；
- 全零预测且有骨架时骨架损失接近 1；
- M1 权重全 1 时与普通公式一致；
- 细半径像素平均权重大于粗半径像素；
- 权重被严格限制在上下限内；
- 单图质量标量放在比值内会抵消的测试成立；
- 空骨架裁块返回有限的 0 骨架项；
- FP32 与 AMP 输出在容差内；
- 梯度只流向预测，不流向预计算标签图。

### 16.3 指标

- 完美预测返回预期值；
- 空标签/空预测符合冻结规则；
- 1 像素平移在不同容差下响应正确；
- 错误桥接显示主召回指标的盲点；
- FOV 外预测不计入；
- 全图计算和分批计算一致；
- 逐图平均与像素汇总刻意构造时能产生不同结果，报告脚本不混淆二者。

### 16.4 训练

- 固定种子短运行可复现到预定数值容差；
- M0/M1/M2 除损失配置外解析配置一致；
- 每次训练日志包含运行 ID、提交、数据哈希和设备；
- 发生 NaN、OOM 或中断时生成失败记录；
- 断点续训不会重复或跳过优化步数。

---

## 17. 交付清单

### 17.1 正结果

- 数据与划分说明；
- M0–M2 九次运行；
- 主比较的逐图与种子结果；
- Precision 约束判定；
- clDice、Dice、AUPRC 等次要结果；
- 盲法案例；
- Fundus-AVSeg 或 HRF 冻结外部测试；
- 局限与禁止外推；
- 可运行推理和独立复现。

### 17.2 负结果

除上述相同的实现、公平性和复现证据外，明确提交：

- 候选未提升、代价过大或不稳定的分类；
- 权重分布、梯度和训练曲线；
- 失败案例和可能机制；
- 唯一修订是否使用及结果；
- 为什么停止继续堆模块；
- 不把负结果写成所有方法普遍无效。

### 17.3 证据不足

- 缺失项及其影响；
- 已完成的图像级结果；
- 不能支持的患者级、物理尺度或质量因果主张；
- 后续取得证据后可执行的具体步骤；
- 仍可复现的内部实验。

### 17.4 外部不可得

- 正式入口、访问日期、许可和失败原因；
- Fundus-AVSeg → HRF 的替代过程；
- 冻结外部推理脚本与标签适配测试；
- 内部测试和分层结果；
- 最终报告中明确“未完成真实外部验证”。

### 17.5 最低课程完成标准

1. 数据、划分和限制可追溯；
2. 可信 M0 基线和至少一个有意义对照；
3. 指标经过合成行为验证；
4. 正式运行不挑结果且失败记录完整；
5. 主结论与证据强度一致；
6. 可运行整图推理；
7. 主要表格可从保存结果重建；
8. 至少一名非主开发组员完成独立复现；
9. 教师额外评分要求取得后另行补入，不回写为已经满足。

---

## 18. 立即可开展的下一批任务

按依赖顺序执行，不需要等待所有任务串行结束：

| 优先级 | 任务 | 前置条件 | 完成标志 |
| --- | --- | --- | --- |
| 1 | 下载并审计 FIVES；生成清单、哈希、质量字段统计和患者映射搜索记录 | 正式数据入口可访问 | G1a 可裁决；G1b 有实际分布 |
| 2 | 用合成二值图实现并测试 Skeleton Recall、粗细权重和主指标 | 无需真实数据 | 损失与指标测试全部通过 |
| 3 | 建立 2D U-Net、512 裁块和滑窗整图推理最小链路 | 少量合法样例或合成图 | 单图端到端可运行，无接缝 |
| 4 | 在学校 8 GB 设备实测 32 通道配置；必要时一次性降级 | 任务 2、3 | 峰值显存、吞吐和完整训练估计可记录 |
| 5 | 审计 Fundus-AVSeg；失败则转 HRF | 正式数据入口可访问 | 标签合并、FOV、许可和文件格式冻结 |
| 6 | 完成 M0 开发基线并冻结训练长度、阈值规则和门槛 | G1a、G2 基础检查 | `freeze_protocol.md` 完成 |
| 7 | M1/M2 短跑与唯一修订裁决 | 任务 6 | G3 决定并锁定正式矩阵 |
| 8 | 完成九次正式训练、内部/外部评测、复现和报告 | G3 | G4 结论与课程交付完成 |

**首先核验项：FIVES 实际文件结构、质量表连接、患者/左右眼映射和划分证据。** 这些信息同时决定划分强度、质量分支和最终结论边界，但不会阻塞合成损失/指标测试与基础代码搭建。

---

## 19. 来源索引

### 数据

- [FIVES 原始数据论文](https://www.nature.com/articles/s41597-022-01564-3)
- [FIVES Figshare 数据记录](https://doi.org/10.6084/m9.figshare.19688169)
- [Fundus-AVSeg 原始数据论文](https://www.nature.com/articles/s41597-025-05381-2)
- [Fundus-AVSeg Figshare 数据记录](https://doi.org/10.6084/m9.figshare.27938034)
- [HRF 官方数据页](https://www5.cs.fau.de/research/data/fundus-images/)
- [CHASE_DB1 官方数据记录](https://researchinnovation.kingston.ac.uk/en/datasets/chasedb1-retinal-vessel-reference-dataset-4/)

### 方法与实现

- [clDice 原论文](https://arxiv.org/abs/2003.07311)
- [clDice 作者代码](https://github.com/jocpae/clDice)
- [Skeleton Recall 原论文](https://arxiv.org/abs/2404.03010)
- [Skeleton Recall 作者代码](https://github.com/MIC-DKFZ/Skeleton-Recall)
- [cbDice 原论文](https://arxiv.org/abs/2407.01517)
- [MONAI 滑窗推理文档](https://docs.monai.io/en/stable/inferers.html)

---

## 20. 最终执行原则

- 数据与标签先核验，划分先于裁块。
- 主线只保留一个问题、一个主要比较和一个外部候选。
- M2 的目标是检验粗细权重相对普通 Skeleton Recall 的增量价值，不以“创新模块”数量评价成功。
- 质量标签用于分层优先于用于加权；低质量不自动等于应获得更大训练权重。
- 所有正式组共享网络、预算、选择规则和调参机会。
- 测试和外部数据只在冻结后使用。
- 候选失败可以完成课程项目；实现错误必须修复或降级范围，不能包装为负结果。
- 主线完成、复现通过、报告可交付后，才考虑一个扩展。
