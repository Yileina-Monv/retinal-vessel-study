# C1 本地检查与小样本开发报告

日期：2026-09-06。设备：RTX 4070 Laptop 8 GB。代码基线：`8ae3b51`。

**结论：组件检查通过；C1 训练暂缓（HOLD）。** 本轮完成几何开发、24 图模型诊断和真实裁块梯度检查。没有新增优化器更新或模型权重，六方法 × 256 步的短程训练尚未运行。此结论不是判定 C1 无效，而是现有证据未达到预设的启动条件。

## 已完成与门槛

| 检查 | 实测 | 预设条件／判断 |
|---|---:|---|
| 训练图资格 | 470/470，100% | 四次原始裁块中至少一次含两组配对；门槛 70%，通过 |
| 完整几何细骨架覆盖 | 64.36% | 逐图宏平均；门槛 20%，通过 |
| 四次裁块实际细骨架接触 | 4.64% | 完整细骨架作分母；不能与完整几何覆盖混用 |
| M0 局部冲突弧段比例 | 8.41% | 逐图宏平均；门槛 10%，未通过 |
| M1 局部冲突弧段比例 | 8.18% | 辅助参照，仍存在同类问题 |
| 真实裁块配对梯度 | M0/M1 各 8 个固定裁块，全部有限 | PAIR 与 SHUFFLE 均产生非零梯度差异，通过 |
| 确定性与真实输入检查 | 10/10 通过 | 含索引增强、空样本、邻链干扰、SCNP、原裁块一致性与 RNG |

“冲突弧段”事先定义为：路径前景分数低于邻侧背景的两两比较比例至少 5%，并列分数计半。24 图按每病种图像 ID 固定取六张，未按模型成绩挑图；这是开发集中的小样本，不是测试集。训练 32 图复用原 smoke_train，抽样计划已固定。

几何采用 32 个连续骨架点，链端留 8 点，裁块留 8 像素；背景距完整标签前景 2–6 像素，最近原骨架归属于同一段，每段 16–128 点。每裁块最多八段，少于两段时辅助项为零。470 图共得到 233,358 个候选段，其中 233,350 个背景资格有效；节点像素 138,994，短链排除 1,111,354 点，链端留白 986,544 点，分段尾部排除 903,770 点。原骨架及原指标未替换。

## 为什么暂缓

局部错误确实存在，但当前结果不足以支持直接启动方法对照。

- M0 冲突比例按 A/D/G/N 分组分别为 6.29% / 6.49% / 18.22% / 2.62%。其中 `train/316_G.png` 达到 76.60%；仅作敏感性说明，暂不计这张图时其余 23 图的宏平均为 5.44%。主结果仍保留全部 24 图，没有删掉难例，也没有事后选择 G 组挽救假设。该图原质量类别为 poor；这不等于已证实标注错误。
- 当前合格几何只覆盖 **46.52% 的 M0 漏检细骨架**（1 像素容差）；M1 为 46.86%。超过一半漏检不在这些合格弧段中，因此“改善全部细血管”的主张过宽。
- 已训练模型上的大部分辅助间隔小于 −5（M0 68.75%，M1 70.31%）；仍有少数困难段。配对与打乱不是同一梯度，但这并不证明配对组织有优化收益。系数 0.1 下，稀疏辅助项与稠密基础项的 logits 梯度范数比跨裁块差异很大，M0 约 0.06–15.89，M1 约 0.006–15.98；这些是 logits 层诊断，不是参数梯度或训练支配性的结论。没有据此私自调系数。

10% 是本轮提前固定的决策假设，不是科学定律。24 张按 ID 选出的图不能证明整个开发集必然低于该值，更不能把 HOLD 写成“C1 已被否定”。下一步若仍保留 C1，应先固定新的检查协议，在全部 118 张开发图确认错误分布与几何范围，再决定是否重开短程训练；不应直接扩大为正式 10,000 步研究。

## 小样本模型诊断

以下均为已有 10,000 步 M0/M1 检查点在本机重新推理，不是 C1 训练结果，也不能代替全部 118 图的既有结果。

| 指标，逐图宏平均 | M0 | M1 |
|---|---:|---:|
| Dice | 89.27% | 89.52% |
| Precision | 92.48% | 91.96% |
| 细骨架召回，1 像素容差 | 70.20% | 72.61% |
| 局部次序反转率 | 2.61% | 2.51% |
| 局部路径通过率，排除邻链干扰后 | 85.82% | 87.00% |

每个模型评估 11,939 条固定弧段。补充审核中，只要走廊混入其他链的一个标签前景像素，即保守剔除其局部连通评价，共剔除 1,032 段；排序指标的分母保持不变。两次推理的排序结果完全一致。该补查在初次 HOLD 后登记，未改变主门槛；初次结果保存在 preflight，补查结果保存在 diagnostics。

局部连通是标签条件下的诊断，不是临床解剖连通真值。额外局部误连率尚未实现，不能宣称误连代价已通过保护门。没有使用 test 200、外部数据或新患者标注。

## 交付与验证状态

新模块位于 [retinal_c1](D:/Desktop/BME/AI/BME/AI临床应用/retinal_c1/pilot.py)。已实现 PAIR、SHUFFLE、POOL、SCNP 损失，以及同初始权重、同裁块、同预算的 M0/M1 六组训练入口。原数据、冻结代码、原 M0/M1 权重及历史协调状态保持不变。当前训练入口在 HOLD 下明确拒绝启动，已实测；训练主循环与 C1 效果仍为 **NOT_RUN**。

- [预注册样本与条件](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/c1_local_v1/protocol.json)
- [综合验收状态](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/c1_local_v1/acceptance.json)
- [470 图几何审计](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/c1_local_v1/geometry_audit.csv)
- [48 次逐图评估](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/c1_local_v1/c1_preflight_images.csv)
- [23,878 条模型—弧段记录](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/c1_local_v1/c1_preflight_pairs.csv)
- [普通固定图的五列审核图](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/c1_local_v1/diagnostics/M0_105_A_examples.png)
- [高冲突图的五列审核图](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/c1_local_v1/diagnostics/M0_316_G_examples.png)
- [10 项检查日志](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/c1_tests.log)

五列审核图依次为原图、标签、路径与邻侧点、融合概率、阈值预测；类别按“高／低冲突 × 通过／未通过”确定，再取最小路径 ID。所有 48 张 FP32 融合概率已保存在 diagnostics，并记录 SHA-256。SCNP 二分类实现已与[作者公式及代码语义](https://jmlipman.github.io/SCNP-SameClassNeighborPenalization/)核对，推理使用原 logits。
