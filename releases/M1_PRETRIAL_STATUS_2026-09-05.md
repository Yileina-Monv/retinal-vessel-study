# 正式试跑前准备状态

2026-09-05：已到达 **M0/M1 扩大开发试跑的入口**。本轮真实训练更新数为 **0**，没有签发真实训练任务，没有运行正式比较。G2 尚未通过。

## 本轮已完成

| 项目 | 验收结果 | 证据 |
|---|---|---|
| 原冻结版本保护 | 父版本 1842 个文件校验通过，未修改原代码、划分与结果 | 父版本 `development_v1` r2；新增版本独立封装 |
| 完整图像预计算 | 600 份骨架、管状区域与半径图；470 训练、118 验证、12 边界案例 | `m1_pretrial_v1/maps.json` |
| 训练集专属标定 | 470 张、10,608,118 个骨架像素；细血管阈值 4 px，参考半径 5.830951690673828 px | `m1_pretrial_v1/calibration.json` |
| M1 损失与配套指标 | FP32 求和、有效骨架样本归约、空值规则、同步几何变换、完整图像推理与疾病宏平均 | 新增 `retinal_m1` 模块 |
| 合成及回归测试 | 20 项通过：6 项冻结版本、4 项 batch、10 项 M1 新测试 | `m1_pretrial_v1/acceptance.json` |
| 真实数据 GPU 检查 | batch 4、512×512、FP16；M0 系数 0 和 M1 系数 0.1/1.0 均有有限损失与梯度；模型权重未变化 | 同上，`optimizer_steps=0` |
| 目标图目视核验 | 普通样本、FOV 边界样本、空标注样本通过 | `m1_pretrial_v1/maps_QC.png`、`QA_APPROVED.json` |
| 跨目录一致性 | 完整包 2465 个载荷文件校验；新绝对路径重新核对版本、环境与真实裁块 | `m1_pretrial_v1_extract_acceptance.json`、`m1_pretrial_v1_relocation_acceptance.json` |

骨架图共约 180.7 MB（十进制）。600 张图共有 13,416,510 个骨架像素，其中 FOV 内边缘 2 px 范围内有 1,944 个，约 0.0145%。主指标保留这些像素，另给内域敏感性结果；这个比例不能证明边界对每张图都无影响。

本轮 GPU 前向/反向检查峰值已分配显存约 3.032 GiB，峰值保留显存约 3.160 GiB。检查没有创建优化器动量状态、没有执行更新，因此它不能替代完整训练显存或稳定吞吐测量，也不能当作 4070 的实测结果。

## 封装身份与跨电脑使用

父版本：`24d1ebfa59ee88379cd3dc06d364cf7b033ff61be067960692d5327d44f812b5`

新增版本：`c1f72270ca89a06286ebc6b196d2e4302ff2c67c00e6fb2153fe2eca256e218b`，绑定 621 个新增文件。

完整包：`.runtime/transfer/m1_pretrial_v1_full.zip`，1,530,361,584 字节，约 1.53 GB（十进制）。

SHA-256：`d6b3ee72f89c87220c5b87ff9f70c876ca1430e3cc5e2e41b84c89ee1da51314`

完整包不含主任务登记表、训练运行目录或本机虚拟环境。另一台电脑解压到新目录，安装固定环境后执行 `python -m retinal_m1.verify`。相同数据与目标图通过字节校验和实际裁块指纹约束；回传结果使用唯一票据、完整检查点和单一登记表，旧进度不能覆盖新进度，未收到的新进度明确标为未知。

本轮跨目录检查在同一台主机进行，不能替代 4070 实机环境验收。跨 GPU 逐位或统计等价性未建立，同一任务跨机器/GPU续训仍禁用。操作说明见 `m1_pretrial_v1/START_HERE.md`。

## 已准备，但还没有运行

新增 M0/M1 开发票据签发、训练和验证入口已经准备。训练任务会固定方法、系数、机器/GPU、种子、有效 batch、计划总步数以及两个版本标识。单台设备上每 64 个成功更新保存，保留恢复状态；结果封包继续沿用原有防冲突流程。

**新增 M1 入口的实际优化更新和暂停/恢复联调尚未执行。** 它是下一次开发试跑起始阶段的检查；不能把原 M0 的恢复测试直接当成 M1 已验证。正常的首次试跑还需要验证实际学习曲线、完整验证集指标和吞吐，不能把本轮随机初始化网络的损失当作模型成绩。

## 目前所处节点

文献与数据审计 → M0 工程验证 → 开发数据/迁移冻结 → **M1 运行前准备完成（当前位置）** → M0/M1 扩大开发试跑 → G2 正式协议冻结 → 正式重复实验。

对应之前最近两周计划，已经完成“第 1–3 天：骨架预计算、损失/指标和边界测试”的工作包，下一步进入“第 4–7 天：M0/M1 扩大开发试跑”。这些是计划工作包的位置，不是已经流逝的天数。

尚未裁定：正式训练步数、最终骨架系数、最终概率阈值、Precision 下限以及后续 M2 是否保留。它们需要开发试跑证据，当前配置继续保存为 null/未冻结。官方 test 和外部图像未用于本轮。

## 首次课程汇报

现有内容已足以支撑第一阶段“文献调研和项目背景介绍”。建议围绕任务背景、四篇核心文献、FIVES 与疾病亚组、研究问题、比较设计和现有工程可行性展开。当前还没有课程模板内的最终 PPT；初期汇报不需要等待正式比较结果。

已整理 `课程要求与算力安排_2026-09-05/首次汇报内容与就绪判断.md`：10 页、600 秒内容安排，附可以直接使用的进度陈述、5 个问答及需要避免的夸大说法。剩余提交准备是套用统一模板、填写真实组员与分工、排版和演练，并确认汇报批次。
