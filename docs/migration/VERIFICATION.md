# 公开副本迁移验证

日期：2026-09-07。

- 97 个 Python 文件语法解析通过；当前入口页的本地链接存在。
- M0/M1 基础测试及不依赖真实资产的 C1 几何/目标测试共 **23 项通过**。
- 首次完整运行相关 25 项测试时，有 2 项 C1 测试因缺少 `.runtime/c1_local_v1/protocol.json` 失败。它们依赖私有本地计划/数据，未在公开副本伪造输入或修改为自动通过；本次基础测试明确排除这两项。
- 公开清单扫描未发现常见凭据串、用户目录、设备 UUID、内网地址、原始数据目录、模型权重和概率数组。
- 600 张 PNG 位于当前开发版本的 `fov/`，属于项目生成的视野掩膜；其他纳入 PNG 为统计曲线或本次测量概览。未迁入眼底照片、标签或局部病例展示。
- 原代码及未改动的结果文件用逐文件 SHA-256 核对；`import_manifest.csv` 区分原始与公开副本摘要和文档更新。
- 新仓库使用独立初始历史；原私有仓库与本机训练资产保留。

这是公开副本的迁移与基础检查，不是新电脑环境安装验收、完整冻结包恢复、模型推理、训练或临床有效性验证。原始最小测试报告中的输入哈希核验是历史执行证据，不表示公开仓库包含那些输入。
