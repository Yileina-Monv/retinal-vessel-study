# 小组协作约定

1. 从 Issue 领取一个具体任务，填写负责人、预计交付和时间预算。
2. 从 `main` 创建任务分支，例如 `analysis/threshold-protocol`；一次 PR 处理一个问题。
3. PR 说明改了什么、怎样验证、结果能支持什么、尚缺什么；附相关 Issue 编号。
4. 至少请一位组员复核后合并。已验收结果更新 `STATUS.md`，然后关闭 Issue。

## 实验结果怎么同步

- 必须包含任务 ID、数据/代码版本、种子、阈值、实际完成样本数、运行时间和失败状态。
- 同步逐图指标、汇总、图表和报告；模型权重、原图、标签与概率缓存不放 Git。
- 原始实验文件不覆盖，修订用新目录并注明父结果；禁止删除不好看的病例或失败运行。
- 公开表中的病例键是公开数据集图像编号，不能添加真实姓名、联系方式或自行推测的患者身份。
- 哈希校验通过、代码能运行、模型有效、临床有效是不同层级，分别报告。

## 训练前

先明确实验目的、终点、保护指标、样本资格与预算。Issue 不是训练票据，GitHub 同步也不是跨机器运行锁；现有机器绑定和单一协调端规则仍适用。不要复制旧机器运行目录来绕过校验。

## 公开提交范围

不要提交 `.env`、登录凭据、本机路径/设备编号、第三方课件、私有 Release 附件、下载包、原始医学图像、模型、概率图、虚拟环境和临时输出。新增图像材料前核对来源与允许分享范围。

公开可读不等于具有直接写入权限。管理员可在仓库 Settings → Collaborators 邀请组员；没有写权限也可 fork 后提交 PR。
