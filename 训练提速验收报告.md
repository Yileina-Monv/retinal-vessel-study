# 本机训练提速验收

2026-09-06，RTX 4070 Laptop 8 GB。**通过：保留两线程磁盘缓存方案。** 完整训练循环耗时下降约 55–60%，实际监督数据及训练状态保持一致。C1 小样本入口默认使用已验收缓存，可用 `--loader original` 回退。

| 实测项目 | 原流程 | 缓存流程 | 结果 |
|---|---:|---:|---|
| M1，128 步完整训练 | 0.607 秒/步 | 0.246 秒/步 | 2.47 倍速度，耗时减少 59.5% |
| C1 PAIR，64 步完整训练 | 0.689 秒/步 | 0.310 秒/步 | 2.23 倍速度，耗时减少 55.1% |
| 四图读取＋整图推理，批量 2 | 1.475 秒/图 | 1.336 秒/图 | 约快 1.10 倍，概率逐像素一致 |

M1 原流程进行了两次 128 步计时；缓存比较两线程和四线程，四线程没有进一步缩短完整训练，故选两线程。CPU 单独读取测试按正反顺序各一轮，128 个裁块的全部数据指纹一致。上述时长包含训练中的输入读取、指纹、前反向与更新，不含单独列出的启动校验及最终保存。

## 改动与代价

将已核对源哈希的图像、标签、FOV、辅助图保存为未压缩数组，训练时按需映射并裁块，避免反复解码和解压。裁块、增强、归一化、损失、学习率、模型和有效 batch 均沿用原实现。C1 的路径与背景数组启动时校验并缓存，仍使用原独立随机流抽样。

- 磁盘缓存约 **31.08 GB**，覆盖 470 张训练图和固定 24 张开发图；建立耗时 **103 秒**。
- 每次启动先校验所需缓存文件的完整哈希。128 步测试所需数据一次校验约 **28 秒**；C1 的 64 步测试连同几何准备约 **19 秒**。这部分没有藏进“零成本预处理”。正式长任务按全量缓存校验另计；不把数据全部装入内存。
- 缓存训练的 CUDA 保留显存约 **3.53 GB**，本轮进程私有内存峰值不超过约 **8.6 GB**；持续保留原资源限制。
- 缓存放在项目的 `.runtime/performance_4070_v1/decoded`。原数据和原 10,000 步 M0/M1 权重未修改，冻结的原训练模块未修改。

## 一致性与未采用方案

128 步的原流程、两线程缓存、四线程缓存，以及缓存 64 步保存后恢复到 128 步，模型、优化器、AMP 状态、随机状态、逐步损失和实际输入指纹全部完全一致。PAIR 的两次 64 步测试连监督点指纹也完全一致。另有 10 项 C1 回归检查全部通过。

推理批量从 2 改为 4 没有稳定提速，四张图出现最大约 0.00357 的概率差异、合计 81 个像素的阈值分类变化，因此未采用。最终仍使用批量 2。最后复测也确认缓存整图标签使用可写副本，消除了只读数组转换警告；输入和概率保持一致。

本轮共执行 **768 次工程测试更新**，检查点独立存放在性能测试目录。它们只证明实现和速度，不是候选效果实验；未开启正式研究任务，也未改写此前的 C1 研究判定。

## 更新后的用时预算

按本轮短程速度线性外推，单次 10,000 步的 M1 纯训练约从 **1.69 小时降至 0.68 小时**；PAIR 约从 **1.91 小时降至 0.86 小时**。例行验证、CPU 结构指标、终点测试、外部评价和重跑储备另计。

核心五方法 × 三种子的正式研究，可先将此前 **40–60 小时**的预算调整为 **25–40 小时**。这是规划估计：尚未实测 10,000 步连续热稳定性、SCNP/POOL 全程成本或完整 15 条轨迹。正式全 118 图验证前，需要扩展当前的 24 图验证缓存和对应评价准备；本轮已覆盖全部 470 张训练图。

## 证据与复现

- [机器可读验收](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/performance_4070_v1/acceptance.json)
- [CPU 正反顺序对照](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/performance_4070_v1/cpu.json)
- [原流程训练](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/performance_4070_v1/training/original128_a.json)
- [两线程缓存训练](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/performance_4070_v1/training/cached128_w2_a.json)
- [缓存断点恢复](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/performance_4070_v1/training/cached_resumed128.json)
- [PAIR 缓存训练](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/performance_4070_v1/c1_training/pair64_cached.json)
- [最终推理对照](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/performance_4070_v1/validation.json)
- [C1 回归检查](D:/Desktop/BME/AI/BME/AI临床应用/.runtime/performance_c1_tests.log)

在项目目录可运行以下独立性能检查，标签必须换成未使用的新名称：

```powershell
.\.venv\Scripts\python.exe -m local_deployment.performance gpu --loader cached --workers 2 --steps 128 --tag new_probe
```

这条命令只运行 128 步工程检查。C1 的小样本入口已接入缓存选择，但研究启动条件仍由它自己的协议控制；性能验收不自动启动研究。
