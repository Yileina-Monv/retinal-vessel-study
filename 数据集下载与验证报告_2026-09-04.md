# HRF 与 Fundus-AVSeg 下载、验证结果

范围说明：本文记录两个外部候选数据集。800 张图像的核心开发集 FIVES 已另行完成下载与核验，见 [FIVES 核心数据报告](FIVES核心数据集下载与验证_2026-09-04.md)。

核验日期：2026-09-04。文件审计完成时间：北京时间 16:48。

**两套数据都已完整落地，并完成全量文件级验证。HRF 来自官方站点；Fundus-AVSeg 使用 Kaggle 公开镜像，尚未证实它与 Figshare 原包逐字节一致。**

当前可以开始本地读取、预处理和最小管线测试。此次没有训练模型、运行临床评测或改动代理配置。

## 1. 为什么返回 403

Figshare 的页面、API 和下载入口都返回 HTTP 403，响应头为 `Server: awselb/2.0`，正文只有 118 字节的通用 HTML。下载入口没有产生正常文件重定向。浏览器打开数据集页面也显示 `403 Forbidden`。

这表明失败发生在下载文件之前，响应来自站点的 AWS 网关层。**更可能是网关访问策略、防护规则或当前出口 IP 相关限制；具体哪条规则触发，服务端没有说明，不能下定论。** 这不是已经下载到的 ZIP 损坏，也不能据此认定数据集需要付费、登录或人工申请。[Figshare 官方公开数据政策](https://help.figshare.com/article/data-access-policy)说明，公开发布的内容可无需登录下载。

官方入口还被网页检索工具解析为 S3 临时下载地址，但返回的地址有效期仅 10 秒，签发时间已经过期。直接去掉签名参数访问 S3，则返回 `AccessDenied`；该地址不能当作永久直链。Windows curl 的本地 TLS 错误是另一个问题，没有把它混同为 Figshare 403 的原因。

解决方式：沿用现有代理，HRF 走官方文件服务器；Fundus-AVSeg 走公开 Kaggle 镜像，经 Google 存储下载成功。**下载障碍通过替代来源解决了，Figshare 原站的 403 本身没有被修复。**

详细证据：[HTTP 诊断记录](data_checks/full_download_2026-09-04/http_diagnosis_evidence.json)。先前的失败与最小测试记录仍保留，不以本次结果覆盖历史。

## 2. 数据在哪里

| 数据集 | 可直接读取的解压目录 | 完整内容 | 下载包大小 |
|---|---|---|---:|
| HRF | [datasets/HRF](data_checks/full_download_2026-09-04/datasets/HRF) | 45 张图像、45 张血管标注、45 张 FOV 掩膜 | 9 包合计 76,315,721 字节，72.780 MiB |
| Fundus-AVSeg | [datasets/Fundus-AVSeg](data_checks/full_download_2026-09-04/datasets/Fundus-AVSeg) | 100 张图像、100 张标注、metadata.xlsx、training.txt、testing.txt | 212,778,115 字节，202.921 MiB |

所有原始图像、标签和表格均保留下载后的原内容。压缩包与解压数据已配置 Git 忽略；没有遗留未完成的 `.part` 文件。

HRF 的 3 个 healthy 压缩包复用先前下载，保存在 `data_checks/minimal_download_2026-09-04/archives/`；其余 6 包和 Fundus 镜像包保存在 `data_checks/full_download_2026-09-04/archives/`。解压后的数据已统一到上表目录。

## 3. HRF：完整性通过，有两项原始标签特性需要记录

来源：[HRF 官方数据页](https://www5.cs.fau.de/research/data/fundus-images/)。只下载血管分割数据，包括 healthy、glaucoma、diabetic_retinopathy 各 15 组；没有混入独立的图像质量数据集。

- 9 个压缩包全部通过 ZIP CRC 校验，记录 SHA-256；这些是本地完整性指纹，不冒充官方公布的校验值。
- 45 组文件名一一对应，135 个文件全部解码成功，尺寸均为 3504×2336。
- 未发现图像文件的字节级完全重复。
- FOV 原文件是 RGB，三个通道相同，取值为 0/255。只在审计内存中取单通道，未改写原文件。
- 42 张血管标注只含 0/255；`11_h`、`12_h`、`13_h` 分别有 599、1,584、1,119 个取值介于 0 与 255 之间的像素，合计 3,302 个。
- 37 组中存在少量非零血管标注落在 FOV 外，合计 4,166 个像素，按原始标注 `>0` 统计。

这些发现不影响文件完整性结论，但意味着后续不能无检查地假设全部标签已严格二值化。审计保留了 `>0`、`>=128`、`==255` 的像素计数用于了解差异，**没有选择训练阈值、修改标注或据此筛除图像**。

证据：[下载与压缩包校验](data_checks/full_download_2026-09-04/hrf_download_evidence.json)、[全量文件审计](data_checks/full_download_2026-09-04/hrf_format_evidence.json)、[逐样本清单](data_checks/full_download_2026-09-04/hrf_manifest.csv)。

## 4. Fundus-AVSeg：镜像全量文件验证通过，原包同一性未确认

官方归属：[Fundus-AVSeg Figshare 记录](https://figshare.com/articles/dataset/Fundus-AVSeg/27938034)，记录版本 2，下载文件 ID `54093641`。实际下载来源：[Kaggle FundusDataset](https://www.kaggle.com/datasets/darksoul007fedsdfds/fundusdataset)，发布者 `darksoul007fedsdfds`，镜像版本 1。镜像版本号与 Figshare 版本号属于不同平台，不能相互等同。

- 压缩包含 203 个文件，ZIP CRC 全部通过。HTTP 声明长度与落盘长度相符，本地 MD5 与该次 Google 存储响应的 ETag 相符。
- 100 张图像与 100 张标注名称完全对应，全部 PNG 解码成功；每对尺寸一致。
- 分辨率分布：79 对为 1280×1280，21 对为 2656×1992。
- 标注只出现黑、红、蓝、绿、白五种颜色；未知颜色像素数为 0。对应背景、动脉、静脉、交叉和类型不确定的血管，与官方描述相符。
- 元数据 100 行，没有缺失字段；疾病为正常 40、DR 20、AMD 20、青光眼 20；质量为高质量 83、低质量 17。
- `training.txt` 80 项、`testing.txt` 20 项，无重复、无交集，合并后覆盖全部 100 张。这里只核对原始划分文件，没有把这个划分直接用作本项目的训练或测试安排。
- 未发现图像文件的字节级完全重复。
- **`eye id` 列实际只有 `left` 和 `right`，分别为 55 和 45。它是眼别，不是唯一患者 ID；不能用它证明患者独立性。**
- 原始文件中没有单独的 FOV 掩膜。若后续指标需要 FOV，生成规则与检查方式在开工时统一确定。

### 来源验证的边界

镜像 ZIP 的 SHA-256：

```text
12a5a9e579fbf6086a10434d260c79641f1a98c64b882a4c8bfb152ad0542c22
```

[torch-em 的固定提交](https://github.com/constantinpape/torch-em/blob/2c01ef9c0f8c274b217f1708e71b0a2732613ac5/torch_em/data/datasets/medical/fundus_avseg.py)为 Figshare 文件 `54093641` 记录了另一个 SHA-256：

```text
6db5ff43c4e9c25aa93093aa295c67b10fa0c089ac650df6665c7a6bbae9539f
```

两者不同。重新打包就可能改变 ZIP 哈希，所以差异本身不能证明图像被改动；但同样不能证明镜像完整保留了原包内容。该参考值来自第三方开源加载器，不是作者签发的校验声明。目前已确认的是**镜像传输完整、结构与公开描述相符、全部文件可读且配对正确**；尚未完成与官方原包解压内容的逐文件比对。

因此，当前镜像足以支持已讨论的最小读取与管线测试；如果最终研究继续使用它，应如实记录镜像来源和版本，并披露这项同一性限制，或在原站可访问后补做逐文件比较。

证据：[下载记录](data_checks/full_download_2026-09-04/fundus_download_evidence.json)、[全量审计](data_checks/full_download_2026-09-04/fundus_format_evidence.json)、[元数据审计](data_checks/full_download_2026-09-04/fundus_metadata_evidence.json)、[逐样本清单](data_checks/full_download_2026-09-04/fundus_manifest.csv)。

## 5. 对当前粗方案的影响

两套数据的本地可获得性和基本读取问题已解决，可以继续固定基础模型、开展独立增量研究的粗方案。此次没有改动基础研究设计，也没有提前决定损失函数、阈值、FOV 或细血管定义。

后续开工时处理三件事即可：HRF 原始灰度标注和 FOV 边界规则；Fundus 外部测试所需的 FOV 与血管合并规则；数据来源和患者独立性能够支持到什么程度。**文件验证通过不等于临床标注正确，也不等于已经完成外部模型验证。**
