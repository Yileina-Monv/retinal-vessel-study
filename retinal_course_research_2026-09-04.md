# 眼底血管分割课程项目：实施前系统调研与关键裁决

- 调研完成日期：2026-09-04
- 项目类型：三人课程项目，日历窗口 10 周
- 调研对象：FIVES 开发数据、骨架/粗细加权损失、图像质量分支、外部验证、评价与实施关卡
- 配套实施规格：[`retinal_course_execution_spec_2026-09-04.md`](retinal_course_execution_spec_2026-09-04.md)
- 总体建议：**GO——推进课程项目；HOLD——质量加权与患者独立性强主张；不以候选方法取得提升作为项目存续条件**
- 证据标记：`原始资料明确报告`、`文件实测`、`有条件推断`、`设计建议`、`未能核实 / UNAVAILABLE`

> 本报告是实施前研究与裁决，不是数据审计、代码完成或实验结果。公开数据压缩包、训练日志、模型输出和 GPU 速度均未在本轮形成可复核实测结果。

## 1. 执行摘要

### 1.1 五项最终建议

| 项目 | 推荐决定 | 证据强度与边界 |
| --- | --- | --- |
| 唯一主问题 | 检验“按标签局部管径加权的 Skeleton Recall，能否提高**人工标签支持的细血管骨架召回**，同时限制整图 Precision 下降” | `设计建议`。与 FIVES 标签和可计算指标相容；不能表述为恢复真实血管、真实连续性或临床有效性 |
| 默认方法 | 固定 2D U-Net；M0 为 BCE + soft Dice，M1 加普通 Skeleton Recall，M2 用粗细加权 Skeleton Recall 替代普通版本 | `原始资料明确报告 + 设计建议`。普通对照明确为 Skeleton Recall Loss；M2 是本报告提出的轻量组合，不宣称首次或已有效 |
| 质量权重 | **从必做主线移除**；保留质量分层评价。仅在 FIVES 文件审计通过后作为 M3 探索分支 | `原始资料明确报告 + 有条件推断`。FIVES 的三项质量标签是整图图像质量，不是局部可见性或标签置信度；论文还提示质量、疾病与血管像素比例可能相关 |
| 主外部候选 | Fundus-AVSeg，定位为**明显域变化下的冻结外部测试**；HRF 为备选；CHASE_DB1 为可选压力测试 | `原始资料明确报告`。Fundus-AVSeg 有 100 名患者、不同医院与设备、完整血管类别可合并，但文件像素值与 FOV 尚未实测，不能包装成近似条件测试 |
| 最小正式实验 | M0、M1、M2，各 3 个匹配随机种子，共 9 次完整训练；主比较 M2−M1，M1−M0 为背景比较 | `设计建议`。M3 不默认进入；若增加 M3，也不能仅凭累计设计宣称粗细与质量存在交互作用 |

### 1.2 GO / PIVOT / HOLD / STOP 的适用对象

| 状态 | 本项目中的含义 | 当前对象 |
| --- | --- | --- |
| GO | 证据足以进入下一阶段，但仍需本地验收 | 主问题、M0–M2 设计、FIVES 文件获取与审计、指标单元测试、Fundus-AVSeg 优先核验 |
| PIVOT | 改变方法或缩小主张，不停止课程项目 | 将“恢复真实连续性”改为“标签支持的细血管骨架召回”；将质量权重降级为探索；必要时只完成 M0–M1 |
| HOLD | 缺失信息会影响特定分支或结论，其他工作可继续 | 患者级划分、质量加权、外部 FOV/颜色映射、768 裁块、家中 RTX 5080 持续可用性 |
| STOP | 停止某一候选修改或无效实施，不等于停止课程项目 | 指标/标签错位未修复的运行、超过一次无明确依据的候选修订、利用测试成绩选阈值或数据 |

### 1.3 最大不确定性

最大不确定性不是网络结构，而是**证据单位**：FIVES 论文报告 800 张图来自 573 名受试者，但公开记录中尚未核实匿名患者与左右眼映射。若该映射不可得，项目仍可完成可信的图像级课程实验，但内部结果不能写成患者独立泛化或新患者测试。FIVES 的官方资料同时明确：每只被纳入的眼睛只留一张照片，但每名患者检查双眼；这不能自动推出官方 600/200 划分按患者隔离。[FIVES 原始数据论文](https://www.nature.com/articles/s41597-022-01564-3)

---

## 2. 证据范围、访问与文件核验记录

### 2.1 本轮覆盖

本轮检索覆盖至 2026-09-04，优先顺序为原始数据论文、正式数据页、会议/期刊原文、作者仓库和官方文档。重点检索了：

- FIVES 的采集、标注、质量字段、公开划分、患者数量、许可和文件结构；
- clDice、Skeleton Recall Loss、cbDice、视网膜血管宽度权重的定义、实现、依赖、许可和反证；
- Fundus-AVSeg、HRF、DR HAGIS、CHASE_DB1 的来源独立性、患者结构、标签、许可和取得条件；
- 滑窗推理、统计单位、阈值冻结、负结果和复现交付。

这不是穷尽式系统综述，不支持“首次提出”或“没有任何相同工作”的绝对新颖性结论。

### 2.2 访问与核验状态

| 对象 | 论文/正式页 | 下载入口 | 文件字节取得 | 文件内容实测 | 本报告可使用的结论 |
| --- | --- | --- | --- | --- | --- |
| FIVES | 可访问；论文与 Figshare 记录均可读 | Figshare 显示约 1.64 GB、CC BY 4.0 | 未完成 | **无** | 可引用论文报告的 800 图、573 人、2048×2048、600/200、质量字段和标注规则；不能报告本地文件计数、哈希、质量交叉表或患者映射 |
| Fundus-AVSeg | 2025 Scientific Data 论文与 Figshare v2 可访问 | Figshare v2 显示约 202.91 MB、CC BY 4.0 | 当前环境下载未成功 | **无** | 可引用 100 图/100 患者、两类分辨率、五色标签、质量类别和元数据字段；实际 RGB 值、异常像素、FOV 和文件对应仍待审计 |
| HRF | FAU 官方数据页可访问 | 官方页给出约 73 MB 整包及分项下载 | 未完成 | **无** | 可引用 45 图、三类各 15、二值标签、部分 FOV 掩膜和 CC BY 4.0；尺寸、患者映射、相机/FOV 冲突待核验 |
| CHASE_DB1 | Kingston 官方记录可访问 | 官方记录给出 2.4 MB ZIP 与 CC BY 4.0 | 当前环境收到访问失败 | **无** | 可引用 14 名儿童双眼、28 图、两观察者、手持相机、30°和采集尺寸；文件名和观察者映射待审计 |
| Skeleton Recall 代码 | 论文与作者仓库可访问 | GitHub 公开 | 未克隆运行 | **无运行实测** | 可引用预计算 tubed skeleton、soft recall、Apache-2.0 和 nnU-Net 集成方式；不能声称已适配普通 U-Net |
| clDice / cbDice 代码 | 论文与作者仓库可访问 | GitHub 公开 | 未克隆运行 | **无运行实测** | 可比较定义、依赖和许可；不能声称当前环境可直接运行 |

数据页与来源：

- [FIVES 论文](https://www.nature.com/articles/s41597-022-01564-3)；[FIVES Figshare DOI](https://doi.org/10.6084/m9.figshare.19688169)
- [Fundus-AVSeg 论文](https://www.nature.com/articles/s41597-025-05381-2)；[Fundus-AVSeg Figshare DOI](https://doi.org/10.6084/m9.figshare.27938034)
- [HRF 官方数据页](https://www5.cs.fau.de/research/data/fundus-images/)
- [CHASE_DB1 官方数据记录](https://researchinnovation.kingston.ac.uk/en/datasets/chasedb1-retinal-vessel-reference-dataset-4/)

### 2.3 当前明确不可用的信息

以下均为 `未能核实 / UNAVAILABLE`，且不得在后续报告中改写成已完成事实：

1. FIVES 压缩包哈希、实际目录计数、图像—标签逐项对应、唯一像素值、损坏文件、FOV 掩膜是否存在。
2. FIVES 的匿名患者编号、左右眼映射以及官方 600/200 是否患者独立。
3. 所选 480/120 开发划分中的三项质量组合、质量×疾病交叉分布、低质量样本数与标签可用性。
4. Fundus-AVSeg v2 五色标签的实际 RGB 值、是否有抗锯齿/异常值、`eye ID` 的实际格式和 FOV 规则。
5. HRF 分割集的患者映射、当前文件尺寸，以及文献中相机和 45°/60°视野说法的版本对应。
6. 学校 RTX 40 系 8 GB 设备的具体型号、功耗、512/768 裁块显存峰值与单次训练时间；家中 RTX 5080 是否可持续远程使用。
7. 课程评分细则、必须展示的指标与演示形式。

### 2.4 冲突与限制的处理原则

- 论文报告、数据页存在、下载成功、文件核验和代码运行是五个不同层级。
- 不从文件名猜患者，也不将“没有像素重复”当成患者独立。
- 不将不同来源的图像相关性、患者重叠、标注偏差、设备域变化和质量差异混为同一问题。
- 外部集可用于证明“在另一个公开来源上的表现”，不能凭不同医院名称证明不存在个体重叠，也不能自动称为近似条件新患者测试。
- 任何数据集中的像素宽度只表示该公开文件的数字尺度；没有物理标定时，不换算为真实微米管径。

---

## 3. R1：主问题与可成立的主张

### 3.1 三个候选问题比较

| 候选问题 | 必要图像与标签 | 必要评价证据 | 公开数据支持度 | 主要风险 | 裁决 |
| --- | --- | --- | --- | --- | --- |
| 减少细血管遗漏 | 原图、二值血管标签、可重复的局部粗细定义 | 标签骨架上的细血管召回、Dice、Precision、分层案例 | **较强**。FIVES 有高分辨率二值标签，但只支持公开像素尺度 | “细”没有物理尺度；标签可能不含不可见结构 | **主线** |
| 改善结构保持、减少错误断开 | 除上述外，还需可信拓扑参考、断开定义及可见性判断 | clDice、骨架覆盖、断开/桥接人工复核 | **中等偏弱**。可评价标签一致性，但 FIVES 明确不为保持连续性补画 | 标签断开不等于模型错误；连接更多可能产生假桥 | **次要机制分析** |
| 改善低质量图像中的细血管，同时限制误检 | 足量低质量图、可靠质量标签、低质量下仍可信的血管标签 | 质量分层细血管召回与 Precision、质量×疾病控制 | **尚不足**。FIVES 有三项整图质量标签，但开发分布未实测，且论文提示青光眼与照明/血管比例相关 | 图像不可见、标签遗漏与疾病差异会被误当作“更难样本” | **评价分层；权重仅探索** |

### 3.2 为什么不能以“恢复真实血管连续性”为主张

FIVES 标注要求以图像中可见外观为依据；当血管看似断裂时，标注者被明确告知不要仅为了维持血管连续性而修改。每张图由两名初级标注者完成，初始结果取共同标注像素，再由资深人员复核。[FIVES 论文：标注流程与连续性规则](https://www.nature.com/articles/s41597-022-01564-3)

因此，至少要区分四种情形：

| 情形 | 可观察现象 | 模型连接更多是否正确 | 本项目能否自动判真 |
| --- | --- | --- | --- |
| 预测造成的断裂 | 标签和图像均支持一段血管，预测中间缺失 | 通常可能是改进 | 可由标签骨架召回提示，但仍需看图确认 |
| 人工标签本身断开 | 图像可能模糊，标签按规则保留断开 | 连接可能违背参考标准 | 不能仅靠预测拓扑判断 |
| 图像不可见区域 | 血管被反光、病灶、低对比或遮挡影响 | 模型补出可能是真结构，也可能是幻觉 | 无额外成像或专家复核时不可判真 |
| 投影交叉与邻近分支 | 两条血管在二维图像交叉或非常接近 | 错误桥接会提高局部覆盖却改变图结构 | clDice、召回和 Dice 均可能漏检，需案例复核 |

所以本项目可以陈述“减少相对于人工标签的骨架漏检”或“提高标签支持的中心线覆盖”，不能陈述“恢复真实解剖连续性”。

### 3.3 总体 clDice 与细血管改善之间缺少什么

总体 clDice 衡量预测/标签骨架与对方分割掩膜的双向覆盖，粗血管和细血管均参与。即使总体 clDice 上升，也不能单独证明：

- 改善发生在细血管，而非较粗主干；
- 改善来自减少断开，而非整体增粗；
- 没有新增假桥或伪支路；
- 不同图像尺度上的“细血管”具有相同物理意义。

因此必须增加一个独立、冻结的细血管骨架召回定义，并用整图 Precision 约束“把预测整体变粗”这一廉价获益路径。clDice 保留为必要次要指标。

### 3.4 推荐研究问题

> 在 FIVES 的固定图像级开发/测试协议下，相对于普通 Skeleton Recall Loss，在相同 2D U-Net、像素损失、裁块、训练预算和阈值规则下加入基于人工标签局部半径的粗细权重，能否提高标签支持的细血管骨架召回，同时不造成预先冻结的整图 Precision 不可接受下降？

### 3.5 可报告的结论模板

**正结果版本：**

> 在本项目冻结的图像级协议和公开像素尺度下，粗细加权 Skeleton Recall 相对于普通 Skeleton Recall 提高了标签支持的细血管骨架召回，且 Precision 代价未超过课程级冻结容限。该证据限于 FIVES 及所选外部数据的参考标签，不能推导真实物理管径、真实血管连续性或临床获益。

**无明显提升版本：**

> 在本项目冻结的训练与评价协议下，粗细加权 Skeleton Recall 未相对于普通 Skeleton Recall 提供稳定且达到预设实践阈值的额外改善，或其召回收益伴随超过容限的 Precision 下降。该结果约束的是本项目定义、权重范围、网络和数据条件，不否定所有拓扑损失、所有粗细权重或临床应用价值。

**证据不足版本：**

> 由于患者映射、外部文件或质量子组证据不足，本项目只能报告图像级内部比较及其限制，不能形成患者独立、近似条件外部验证或低质量专项改善结论。

---

## 4. R2：最接近方法与候选方法的可实现定义

### 4.1 最接近方法对照

| 方法 | 解决的问题 | 核心定义 | 预测骨架 | 标签骨架可预计算 | 公开实现与许可 | 对本项目的影响与限制 |
| --- | --- | --- | --- | --- | --- | --- |
| clDice / soft-clDice | 管状结构的中心线覆盖与拓扑保持 | hard clDice 用预测/标签骨架与对方掩膜的双向覆盖；soft 版本用可微形态学骨架 | soft 损失需要 | hard 指标可以 | [CVPR 论文](https://openaccess.thecvf.com/content/CVPR2021/html/Shit_clDice_-_A_Novel_Topology-Preserving_Loss_Function_for_Tubular_Structure_CVPR_2021_paper.html)；[作者代码，MIT](https://github.com/jocpae/clDice) | 适合作为指标；soft 骨架增加计算和实现复杂度。原论文的理论条件不能自动转移到任意加权变体 |
| Skeleton Recall Loss | 用低开销方式提高标签骨架覆盖 | 对标签骨架膨胀形成 tubed skeleton，在预测概率上计算 soft recall，并与普通像素损失相加 | 不需要 | **可以** | [ECCV 2024 论文](https://arxiv.org/abs/2404.03010)；[作者仓库，Apache-2.0](https://github.com/MIC-DKFZ/Skeleton-Recall) | 轻量、与普通 U-Net 易于重写，适合作为“普通骨架方法”主对照；本身没有直接假阳性惩罚 |
| cbDice | 同时考虑中心线、边界与半径，平衡不同管径 | 在 clDice 系列上加入距离/半径权重 | 需要可微骨架方案 | 部分中间量可预计算 | [MICCAI 2024 论文](https://arxiv.org/abs/2407.01517)；[作者仓库，Apache-2.0](https://github.com/PengchengShi1220/cbDice) | 是与“粗细+骨架”非常接近的先行工作，削弱新颖性主张；实现依赖 MONAI、cuCIM/CuPy 等，课程主线成本较高 |
| Vessel-width weight masks | 专门提高细血管像素的损失权重与评价 | 公开包给出基于半径和距背景距离的 W0/W1 权重及细血管 Precision/Recall | 不要求 | 距离图可预计算 | [项目与说明，MIT](https://pypi.org/project/retinal-thin-vessels/) | 直接证明“基于标签粗细加权”并非空白；目前证据来自较新的工作坊/软件包，需谨慎解释 |
| 整图质量样本加权 | 强化低质量样本或特定损失项 | 每图标量乘样本损失 | 不要求 | 质量标签可预读 | 无单一方法可直接证明 FIVES 上合理 | FIVES 质量是整图三项二值评分，不是局部标签置信度；应先审计，默认不进入主线 |

### 4.2 普通骨架主对照的选择

**选择：Skeleton Recall Loss。**

理由：

1. 原方法只需要预计算标签骨架和膨胀后的 tubed skeleton，不在 GPU 上对预测做可微骨架化，适合 8 GB 设备。
2. 它直接优化“标签骨架被预测覆盖”的召回，与主问题一致。
3. 作者实现和论文定义明确，可在普通二值 U-Net 中用很短的向量化代码重写，不必引入完整 nnU-Net。
4. 它的限制也清楚：连接性结果并非在所有架构/数据上稳定，且骨架召回项本身不惩罚骨架外假阳性。

原论文在 DRIVE 上报告过小幅 Dice/clDice收益，但同一论文的 HRNet 实验也出现 Dice 明显下降而 clDice 上升，说明拓扑导向损失可能以像素精度为代价。[Skeleton Recall 原论文](https://arxiv.org/abs/2404.03010) 2025 年的一篇复核预印本进一步质疑其跨任务稳定性，并从梯度角度指出该项对骨架外像素没有直接作用；该预印本使用的模型、数据与统计也有局限，因此应作为反证而非“推翻”。[复核预印本](https://arxiv.org/abs/2508.11374)

### 4.3 符号与输入

对每个训练裁块，定义：

- 输入图像：$X_n \in \mathbb{R}^{3\times H\times W}$
- 二值标签：$Y_n \in \{0,1\}^{H\times W}$
- 网络 logit：$Z_n=f_\theta(X_n)$
- 预测概率：$P_n=\sigma(Z_n)$
- 有效视野：$F_n \in \{0,1\}^{H\times W}$
- 单像素标签骨架：$S_n=\operatorname{skel}(Y_n\odot F_n)$
- 标签欧氏距离变换：$D_n=\operatorname{EDT}(Y_n\odot F_n)$
- 半径为 2 像素的菱形结构元素：$B_2$
- tubed skeleton：$M_n=\operatorname{dilate}(S_n,B_2)\odot Y_n\odot F_n$

这里的距离和半径均为公开图像文件的像素单位，不是物理管径。

### 4.4 全图预计算粗细图

骨架、距离变换和粗细权重应在**完整标签图**上计算，再与图像同步裁块，避免裁块边缘把一条被切断的粗血管误判为细分支。

在完整图像上，对骨架点定义局部半径：

$$
r(x)=D(x),\qquad x\in S.
$$

对 tubed skeleton 中不在中心线的像素，采用最近骨架点传播半径。设最近骨架投影为 $\pi_S(x)$，则：

$$
\widetilde r(x)=r\!\left(\pi_S(x)\right),\qquad x\in M.
$$

这样同一局部横截面附近的 tubed skeleton 获得近似相同的管径权重，不会因为管壁像素离背景更近而被错误地额外放大。

参考半径只由训练标签确定：

$$
r_{\mathrm{ref}}=\operatorname{median}\{r(x):x\in S_{\mathrm{train}}\}.
$$

推荐的保守粗细权重为：

$$
a(x)=\operatorname{clip}\!\left[\left(\frac{r_{\mathrm{ref}}}{\widetilde r(x)+\epsilon}\right)^p,a_{\min},a_{\max}\right].
$$

初始设计默认值：

$$
p=1,\qquad a_{\min}=0.5,\qquad a_{\max}=2.0,\qquad \epsilon=10^{-6}.
$$

这些数值是 `设计建议`，不是文献已证实的最优值。它们的目的只是限制薄分支权重不超过普通骨架的 2 倍、粗主干不低于 0.5 倍。正式训练前必须用训练/验证数据检查权重分布、梯度规模和少量覆盖图；不得根据测试集改动。

**归一化说明：**加权召回的分母本身对有效骨架权重求和，因此所有权重同乘一个图像级常数会完全抵消。不要把“每图均值归一化”包装成有效的新机制；只需用训练集参考半径与上下限稳定相对权重，并记录分布。

### 4.5 损失定义

像素损失固定为二元交叉熵与 soft Dice 之和：

$$
\mathcal{L}_{\mathrm{BCE},n}
=-\frac{1}{|F_n|}\sum_i F_{n,i}
\left[Y_{n,i}\log(P_{n,i}+\epsilon)+(1-Y_{n,i})\log(1-P_{n,i}+\epsilon)\right].
$$

$$
\mathcal{L}_{\mathrm{Dice},n}
=1-\frac{2\sum_i F_{n,i}P_{n,i}Y_{n,i}+\epsilon}
{\sum_i F_{n,i}P_{n,i}+\sum_i F_{n,i}Y_{n,i}+\epsilon}.
$$

$$
\mathcal{L}_{\mathrm{pix},n}
=\mathcal{L}_{\mathrm{BCE},n}+\mathcal{L}_{\mathrm{Dice},n}.
$$

普通 Skeleton Recall 写成 $1-$ soft recall：

$$
\mathcal{L}_{\mathrm{SRec},n}
=1-\frac{\sum_i M_{n,i}P_{n,i}+\epsilon}
{\sum_i M_{n,i}+\epsilon}.
$$

本报告提出的粗细加权版本：

$$
\mathcal{L}_{\mathrm{WSRec},n}
=1-\frac{\sum_i M_{n,i}a_{n,i}P_{n,i}+\epsilon}
{\sum_i M_{n,i}a_{n,i}+\epsilon}.
$$

三组正式损失：

$$
\mathcal{L}_{M0}=\frac{1}{N}\sum_n\mathcal{L}_{\mathrm{pix},n}.
$$

$$
\mathcal{L}_{M1}=\frac{1}{N}\sum_n\left(\mathcal{L}_{\mathrm{pix},n}+\lambda_s\mathcal{L}_{\mathrm{SRec},n}\right).
$$

$$
\mathcal{L}_{M2}=\frac{1}{N}\sum_n\left(\mathcal{L}_{\mathrm{pix},n}+\lambda_s\mathcal{L}_{\mathrm{WSRec},n}\right).
$$

原 Skeleton Recall 论文只有限测试 $\lambda_s\in\{0.1,1.0\}$。本项目建议在开发阶段用相同短跑预算比较这两个值，按验证 Dice、细血管骨架召回和 Precision 的冻结规则选择一次；M1 与 M2 使用同一个 $\lambda_s$，避免给候选更多调参机会。[Skeleton Recall 原论文与实现](https://github.com/MIC-DKFZ/Skeleton-Recall)

### 4.6 空骨架、无血管裁块和批量归约

- 若某裁块满足 $\sum_i M_{n,i}=0$，该裁块的骨架损失记为 0，并在 `valid_skeleton=false` 中留痕；像素损失照常计算。
- 批量骨架损失只在有有效骨架的样本上取均值，不能将空骨架视为“召回 1”。
- 不用一个跨整个 batch 的总分母，否则含大量血管的裁块会主导小分支裁块。
- 裁块采样必须在所有组中相同；不允许 M2 专门获得更多含细血管裁块。
- 可用 FOV 内采样减少黑边空块，但采样规则在 M0–M2 中完全一致并写入配置。

### 4.7 可选质量权重定义及其局限

FIVES 对每张图给出三项二值评分，1 表示该方面质量较好、0 表示较差。设三项分数为 $s_{n,j}$，则可定义缺陷比例：

$$
z_n=1-\frac{1}{3}\sum_{j=1}^{3}s_{n,j}.
$$

若且仅若质量分支通过审计，可用一个温和的整图标量只作用于骨架项：

$$
q_n^{\mathrm{raw}}=1+\gamma z_n,
\qquad
q_n=\frac{q_n^{\mathrm{raw}}}{N_{\mathrm{train}}^{-1}\sum_k q_k^{\mathrm{raw}}}.
$$

探索性初值可设 $\gamma=0.5$，但必须在运行前冻结。M3 为：

$$
\mathcal{L}_{M3}=\frac{1}{N}\sum_n
\left(\mathcal{L}_{\mathrm{pix},n}+\lambda_s q_n\mathcal{L}_{\mathrm{WSRec},n}\right).
$$

关键点：

1. $q_n$ 必须放在逐图归一化的召回比值之外；若同时乘到分子和分母，它会完全抵消。
2. FIVES 质量标签只能支持整图权重，不能伪造局部质量图。
3. “图像越差越加权”不是已证实规则。不可见结构、交集式标注与低质量可能使标签更保守，强化它反而会放大不确定参考。
4. 论文提示青光眼图像的照明/颜色分数和血管像素比例存在差异；未经质量×疾病审计，质量权重可能实际重加权疾病类别。[FIVES 论文](https://www.nature.com/articles/s41597-022-01564-3)
5. 若不增加“仅质量权重、无粗细权重”的组，累计 M0–M3 只能描述增量，不能估计粗细×质量交互作用。

### 4.8 简短伪代码

```text
for each full training label Y:
    F = build_or_load_fov(Y, image)
    S = skeletonize(Y AND F)
    D = edt(Y AND F)
    M = dilate(S, diamond_radius=2) AND Y AND F
    R = propagate_nearest_skeleton_radius(D * S, S, M)
    A = clip((r_ref / (R + eps)) ** p, a_min, a_max)
    save F, M, A

for each training step:
    X_patch, Y_patch, F_patch, M_patch, A_patch = synchronized_crop_and_transform(...)
    P = sigmoid(model(X_patch))
    L_pix = bce(P, Y_patch, F_patch) + soft_dice(P, Y_patch, F_patch)
    if M_patch has foreground:
        L_s = 1 - sum(M_patch * A_patch * P) / sum(M_patch * A_patch)
    else:
        L_s = 0
    L = L_pix + lambda_s * L_s
    backward_and_update(L)
```

M1 只需把 `A_patch` 设为全 1；M0 不计算骨架项。

### 4.9 最少必要实现检查

| 检查 | 通过条件 | 失败时行动 |
| --- | --- | --- |
| 图像、标签、FOV、骨架、半径图同步 | 随机抽样叠加完全对齐 | 停止训练，修复变换链 |
| 全图计算再裁块 | 同一位置由全图裁出与缓存一致 | 禁止在裁块内重新骨架化/EDT |
| 几何增强 | 水平/垂直翻转和 90°旋转后所有离散图一致 | 暂不使用任意角旋转、缩放、弹性变形 |
| 光度增强 | 只改变图像，不改变标签/骨架/权重 | 修复管线 |
| 空骨架 | loss 有限、梯度只来自像素项、日志可计数 | 修复除零与 reduction |
| 权重界限 | 所有有效值在冻结上下限内，无 NaN/Inf | 检查 EDT、最近点传播、空标签 |
| 梯度路径 | M2 在细骨架位置相对 M1 的绝对梯度增大，骨架外仍由像素项约束 | 若骨架外无像素梯度，说明组合错误 |
| 滑窗拼接 | 常量图、棋盘图和小图的输出无接缝与尺寸偏移 | 修复 padding/坐标/融合，不进入正式评测 |

### 4.10 默认方案与降级方案

**默认方案：**M0–M2；普通 Skeleton Recall 为主要骨架对照，M2 用训练标签半径做有限范围权重；质量只分层报告。

**降级方案：**若粗细图实现、资源或开发证据不通过，完成 M0 对 M1，使用固定细血管骨架指标、总体 clDice、Dice、Precision 和案例审查。该交付可准确定位为 Skeleton Recall 在 FIVES 条件下的复现/适用性评估。

**新颖性定位：**cbDice 已把中心线、边界和半径结合，新的视网膜宽度权重工作也已经公开；所以 QCA-Skel 最安全的定位是“轻量组合与适用条件评估”，而非新损失首次提出。

---

## 5. R3：FIVES 能支持哪些问题

### 5.1 已核实事实

| 事实 | 状态 | 来源与影响 |
| --- | --- | --- |
| 800 张图、573 名受试者，四类各 200 | `原始资料明确报告` | [FIVES 论文](https://www.nature.com/articles/s41597-022-01564-3)。图像数不等于患者数，必须限制独立性主张 |
| 2016–2021，浙江大学医学院附属第二医院；Topcon TRC-NW8，50°，黄斑中心 | `原始资料明确报告` | 有助于描述内部域；不能由相机型号推导真实像素/度或血管微米宽度 |
| 原图与标签均为 2048×2048 | `原始资料明确报告` | 支持原公开像素尺度裁块和整图重建 |
| 官方推荐 600/200，按疾病与质量考虑平衡 | `原始资料明确报告` | 可作为图像级课程协议；患者级隔离仍未知 |
| 质量表有 Train/Test 两页、疾病/图号/三项质量共 5 列；1 好、0 差 | `原始资料明确报告` | 支持分层描述；开发分布必须文件实测 |
| 标注不为维持连续性而补画；两初级标注共同像素后资深复核 | `原始资料明确报告` | 限制真实连续性主张，也提示细血管标签可能偏保守 |
| Figshare 页面显示约 1.64 GB、CC BY 4.0 | `原始资料明确报告` | 可用于课程研究并需署名；本轮未完成文件下载 |
| 患者/左右眼字段是否公开 | `未能核实 / UNAVAILABLE` | 训练可继续，但新患者内部测试主张 HOLD |
| 质量×疾病开发分布、低质量样本数量 | `未能核实 / UNAVAILABLE` | 质量权重不得进入主线 |

### 5.2 图像质量、标签质量、可见性与疾病差异

四者不能互换：

- **图像质量**：FIVES 的照明/颜色、模糊、低对比三项整图二值评分。
- **标签质量**：标注是否准确、一致和完整；FIVES 的图像质量分数不是标签置信度。
- **血管可见性**：局部结构在原图中是否可分辨，可能在同一张图内空间变化。
- **疾病差异**：病灶、年龄、血管密度与成像质量可能共同变化。

因此，低质量组表现更差只能说明该标签分组下性能差异，不能自动归因于传感器噪声，也不能证明增加质量权重会改善泛化。

### 5.3 FIVES 分支决定

| 条件 | 决定 |
| --- | --- |
| 图像/标签/质量表均可取得且一一对齐 | 可进入主线数据准备 |
| 患者 ID 可取得并覆盖全部图像 | 优先按患者分组重新建立 train/validation/test，或至少验证官方 600/200 是否患者隔离 |
| 患者 ID 不可得 | 保留官方 600/200；从 600 内建立固定图像级 480/120 开发划分；明确不支持患者独立内部结论 |
| 质量表可对齐但低质量组合稀少、严重偏向某疾病 | 只做描述性分层，不做训练权重 |
| 质量分布尚可，但抽样显示低质量标签不可辨/明显保守 | 删除质量权重；保留“标签条件下”的分层结果和限制 |
| 质量分布、疾病平衡和标签可用性均通过 | 可启动一次预先定义的 M3 探索，不改变 M0–M2 主比较 |

### 5.4 最小数据清单字段

建议一行对应一张完整图像：

| 字段 | 必需性 | 说明 |
| --- | --- | --- |
| `dataset`, `dataset_version`, `source_url`, `license` | 必需 | 来源与版本 |
| `archive_name`, `archive_sha256`, `extracted_at` | 必需 | 可复现与完整性 |
| `image_id`, `image_path`, `mask_path` | 必需 | 一一对应 |
| `image_sha256`, `mask_sha256` | 必需 | 重复与串位检查 |
| `image_shape`, `mask_shape`, `image_dtype`, `mask_unique_values` | 必需 | 格式审计 |
| `fov_path_or_rule`, `fov_area` | 必需 | 有效评价范围 |
| `disease` | 必需 | 四病种分层 |
| `quality_illumination`, `quality_blur`, `quality_contrast` | FIVES 必需 | 原始 0/1 值 |
| `quality_overall` | 可选派生 | 必须记录派生规则，不覆盖原字段 |
| `patient_id`, `eye` | 尽力获取 | 不可得填 `UNAVAILABLE`，不猜测 |
| `official_split`, `project_split` | 必需 | 保留原划分与本项目划分 |
| `provenance`, `notes`, `audit_status` | 必需 | 冲突、异常、修复记录 |

### 5.5 训练前必须完成的检查

1. 核对压缩包版本、许可、SHA-256、解压文件数与目录结构。
2. 验证 800 对图像/标签一一对应；尺寸、通道、位深和唯一标签值符合预期。
3. 检查缺失文件、损坏图、重复哈希、同一图像跨划分重复。
4. 读取质量表，验证 800 行映射、三列只含允许值，生成 Train/Test 和疾病×质量位型交叉表。
5. 搜索公开文件是否存在患者/眼别字段；没有则明确写入 `UNAVAILABLE`。
6. 先冻结完整图像划分，再生成裁块；同一原图的裁块不得跨集合。
7. 建立或核验 FOV 规则，抽样检查标签是否主要位于 FOV 内。
8. 全图预计算骨架、EDT、tubed skeleton 和权重图；保存参数与代码版本。
9. 随机抽查至少每病种、每质量位型各若干图的图像—标签—骨架—权重叠加。
10. 所有异常必须形成清单；只有影响训练/评价的异常才阻塞主线，其余可带限制继续。

---

## 6. R4：评价、统计与冻结规则

### 6.1 主指标：细血管骨架召回

对完整标签图，在 FOV 内提取标签骨架 $S_Y$ 和距离变换 $D_Y$。以训练标签骨架半径分布的第 25 百分位定义相对细血管阈值：

$$
\tau=Q_{0.25}\{D_Y(x):x\in S_{Y,\mathrm{train}}\}.
$$

细血管骨架集合：

$$
S_Y^{\mathrm{thin}}=\{x\in S_Y:D_Y(x)\leq\tau\}.
$$

把概率图按冻结阈值 $t$ 二值化：

$$
\widehat Y_t=\mathbb{1}[P\geq t]\odot F.
$$

主指标采用标签骨架到预测前景的容差召回：

$$
R_{\mathrm{thin}}(\delta)=
\frac{\sum_{x\in S_Y^{\mathrm{thin}}}\mathbb{1}[d(x,\widehat Y_t)\leq\delta]}
{|S_Y^{\mathrm{thin}}|}.
$$

推荐主容差为 $\delta=1$ 个原图像素，并预先固定 $\delta\in\{0,2\}$ 的敏感性结果。其含义是“人工标签细骨架附近是否出现预测前景”，不是预测骨架是否拓扑正确。整体增粗也能提高该召回，因此 Precision 是强制护栏。

**阈值依据：**第 25 百分位是预注册的相对定义，避免借用跨分辨率不兼容的“宽度小于 3 像素”等通用阈值。正式报告必须给出由训练标签得到的实际 $\tau$，并明确其仅适用于 FIVES 公开像素尺度。外部集使用同一“参考骨架半径第 25 百分位”规则生成各自的描述性细血管层，但不能直接比较不同数据集的 $\tau$ 数值。

### 6.2 必要次要指标

hard clDice：

$$
T_{\mathrm{prec}}=\frac{|S_{\widehat Y}\cap Y|}{|S_{\widehat Y}|+\epsilon},
\qquad
T_{\mathrm{sens}}=\frac{|S_Y\cap\widehat Y|}{|S_Y|+\epsilon}.
$$

$$
\operatorname{clDice}=
\frac{2T_{\mathrm{prec}}T_{\mathrm{sens}}}
{T_{\mathrm{prec}}+T_{\mathrm{sens}}+\epsilon}.
$$

像素 Dice 与 Precision：

$$
\operatorname{Dice}=\frac{2TP}{2TP+FP+FN+\epsilon}.
$$

$$
\operatorname{Precision}=\frac{TP}{TP+FP+\epsilon}.
$$

推荐结果层级：

1. **主指标**：逐图 $R_{\mathrm{thin}}(1)$。
2. **关键护栏**：逐图 Precision。
3. **必要次要指标**：clDice、Dice。
4. **可选稳健性**：AUPRC、$R_{\mathrm{thin}}(0)$、$R_{\mathrm{thin}}(2)$、中/粗半径层召回、疾病和质量分层。

### 6.3 指标计算协议

- 所有正式指标在滑窗拼回后的**完整图像**上计算，不以裁块为统计样本。
- 使用固定的 2D 骨架算法与库版本；标签和预测采用同一算法。
- 主指标不删除短分支，避免把真实末梢血管当噪声。若数据审计确认存在独立标签散点，可在训练集上定义对称清理规则，作为敏感性分析而非事后修图。
- FOV 内计算 Dice、Precision、clDice 和细骨架召回。FOV 缺失时使用预先冻结、与模型输出无关的图像规则生成，并保留可视化审计。
- 若完整图像标签骨架为空，相关骨架指标记为 `NA` 并报告数量；不能自动记 1。正常眼底整图预计不为空，但代码仍需覆盖该情形。
- 不把端点数量差、连通分量差或 Betti 数直接称为“真实断裂数”；它们对交叉、边界、短支路和标签断开敏感。

### 6.4 自动指标漏掉的错误

| 错误 | $R_{\mathrm{thin}}$ | Dice | Precision | clDice | 需要的补充 |
| --- | --- | --- | --- | --- | --- |
| 删除一段细分支 | 通常下降 | 小幅下降 | 可能不变 | 通常下降 | 主指标能捕获 |
| 整体增粗 | 可能上升 | 可能先升后降 | 下降 | 可能变化不大 | Precision 护栏 |
| 两条邻近血管错误桥接 | 可能不变或上升 | 小幅下降 | 下降幅度可能很小 | 可能漏掉 | 预定义案例人工复核 |
| 预测整体偏移 1 像素 | 容差决定 | 下降 | 下降 | 下降 | 报告多容差敏感性 |
| 裁块接缝断裂 | 接缝处下降 | 下降 | 可不明显 | 下降 | 拼接单元测试与接缝热图 |
| 标签自身断开但模型连接 | 可能不变 | 可能下降 | 下降 | 结果难解释 | 原图盲审并标“参考不确定” |

### 6.5 合成单元测试

用小型二值图验证计算行为，不把它当真实有效性证据：

| 合成改动 | 预期主指标 | 预期 Precision | 预期 clDice | 验收目的 |
| --- | --- | --- | --- | --- |
| 从细线中删 3–5 像素 | 明显下降 | 可略升 | 下降 | 骨架漏检灵敏度 |
| 增加错误桥 | 不应被解释为改善证据 | 下降 | 可能变化有限 | 证明需要 Precision 与案例审查 |
| 将血管膨胀 1–3 像素 | 召回可上升 | 下降 | 不一定同步 | 防止“变粗作弊” |
| 全图平移 1 像素 | $\delta=0$ 下降，$\delta=1$ 较稳 | 下降 | 下降 | 容差实现正确 |
| 在滑窗边界切断 | 接缝位置下降 | 可小幅变化 | 下降 | 拼接测试 |
| 空标签/空预测 | 返回预定 NA 或有限值，无 NaN | 返回预定值 | 返回预定值 | 数值稳定性 |

### 6.6 案例抽样与人工复核

在查看正式测试成绩前，固定一个无偏案例面板：每病种 4 张，共 16 张；质量位型尽量覆盖，若某位型不足则如实记录。另可在结果后增加“机制面板”：M2−M1 主指标变化最大正向 4 张、最大负向 4 张，仅用于解释，不代表总体。

两名组员在隐藏模型名称的叠加图上独立标记：

- 标签支持的细分支遗漏是否减少；
- 是否新增假桥、伪支路或明显增粗；
- 是否为 FOV/拼接边界问题；
- 原图与标签是否存在不可见/不确定区域。

分歧保留，不把学生复核称为眼科专家金标准。

### 6.7 模型、阈值与最小有意义改善的冻结

**建议的共同规则：**

1. 每个运行按固定验证 Dice 选择 checkpoint；不按测试结果挑 epoch。
2. 在固定验证集上，以同一阈值网格选择 $t$：在 Precision 不低于冻结下限的候选中最大化 $R_{\mathrm{thin}}(1)$；并列时选 Dice 更高者。
3. Precision 下限、最小有意义召回改善和容许 Precision 下降在 G2 后冻结，只能使用 M0/M1 的验证结果、指标单元测试和盲法叠加审查，不能使用 M2 正式结果或测试集。
4. 由于当前没有基线波动，本文不虚构通用百分比。建议将课程级最小改善设为不小于 M1 三种子验证均值的典型种子间波动，并要求盲法案例中出现可解释的细分支恢复；Precision 容限用同样方法结合假桥/增粗审查确定。
5. 外部数据直接使用 FIVES 冻结 checkpoint 与阈值，不在外部标签上再调阈值。AUPRC 可作为阈值不敏感的补充。

### 6.8 配对统计

- 固定同一测试图集合、同一划分、同一网络和匹配种子；主比较为 M2−M1。
- 对每张图先对三个种子的预测指标取均值，再计算逐图配对差；同时单独报告三个种子对的总体差异，展示训练随机性。
- 若患者 ID 可得，先按患者汇总双眼或使用患者聚类 bootstrap；若不可得，使用图像级配对 bootstrap，并明确其不能估计患者级采样不确定性。
- 95% 置信区间建议用聚类/图像级配对 bootstrap；可附配对置换检验，但不把随机种子、裁块或左右眼当作新增独立患者。
- 不用三个随机种子做 $n=3$ 的独立样本显著性检验来代表临床人群。

解释规则：

| 结果 | 报告方式 |
| --- | --- |
| 估计改善超过冻结实践阈值，CI 主要为正，Precision 合格，种子一致 | 支持本协议下的正结果；仍限制到标签与数据域 |
| 点估计为正但 CI 宽跨零 | 证据不确定，不能称稳定提升 |
| $p>0.05$ | 只能说未获得足够差异证据，不能说等价或绝对无效 |
| 三种子方向冲突 | 优先报告不稳定性，检查优化/阈值，不选择最好种子 |
| 主指标提高但 Precision 超容限 | 判为代价不可接受，不计为主结论正结果 |
| 内部提高、外部不支持 | 报告内部适用性与域变化失败，不把外部结果隐藏 |

---

## 7. R5：最小可行外部验证

### 7.1 候选比较

| 数据集 | 独立来源与患者结构 | 标签与尺寸 | 质量/采集 | 许可与取得 | 角色裁决 |
| --- | --- | --- | --- | --- | --- |
| Fundus-AVSeg | 深圳眼科医院；论文报告 100 图来自 100 名患者，2022 年 1–3 月；新患者队列与新设备来源 | 2656×1992 或 1280×1280；五色：动脉、静脉、交叉、不明类别血管、背景 | ZEISS VISUCAM200 或 Canon；高/低质量，17% 低质量 | Figshare v2，约 202.91 MB，CC BY 4.0；本轮未完成文件下载 | **主外部域变化测试** |
| HRF | 官方页给出 45 图：健康、DR、青光眼各 15；患者映射未核实 | 二值血管标签，提供部分 FOV 掩膜；公开尺寸需文件核验 | 分割集相机/FOV 的来源冲突尚未裁决；另有独立 18 对图质量集，不能混用 | 官方约 73 MB，CC BY 4.0 | **备选**；格式简单但规模小、元数据弱 |
| CHASE_DB1 | 14 名约 10 岁儿童双眼，共 28 图；明确双眼相关 | 采集 1280×960；每图两位独立观察者标签 | 手持 Nidek NM-200-D，30°，非均匀照明/低对比 | 官方 2.4 MB ZIP，CC BY 4.0 | **可选压力测试**，不是近似条件新患者集 |
| DR HAGIS | 英国糖尿病筛查，多设备；原论文报告 39 张不同图 | 高分辨率血管标签；曾有同一图在疾病展示编号中重复的说明 | 多设备、糖尿病和合并病变，域变化大 | 原论文可读；当前数据入口与许可需进一步核验 | 不作为默认备选，除非 HRF 不可用 |

来源：

- [Fundus-AVSeg 原始论文](https://www.nature.com/articles/s41597-025-05381-2)；[Figshare v2](https://doi.org/10.6084/m9.figshare.27938034)
- [HRF 官方数据页](https://www5.cs.fau.de/research/data/fundus-images/)
- [CHASE_DB1 官方记录](https://researchinnovation.kingston.ac.uk/en/datasets/chasedb1-retinal-vessel-reference-dataset-4/)
- [DR HAGIS 原始论文](https://pmc.ncbi.nlm.nih.gov/articles/PMC5299858/)

### 7.2 为什么选择 Fundus-AVSeg

它优于 HRF/CHASE 作为第一核验对象的原因是：

- 100 图来自 100 名患者，疾病构成与 FIVES 相似：正常 40，DR、AMD、青光眼各 20；
- 来源医院与相机设备不同于 FIVES，能提供清楚的域变化证据；
- 论文报告完整元数据、两类图像质量和五类血管标注；
- CC BY 4.0 且 Figshare 有版本记录。

但它**不是与 FIVES 条件匹配的新患者测试**。相机、分辨率、机构、时间与标签规则均变化，因而外部结果应解释为综合域迁移压力，而不是纯患者泛化。

### 7.3 Fundus-AVSeg 二值化规则

论文将红色定义为动脉、蓝色为静脉、绿色为动静脉交叉、白色为类别不明血管、黑色为非血管。[Fundus-AVSeg 论文](https://www.nature.com/articles/s41597-025-05381-2)

总血管分割应在实际像素审计后采用：

$$
Y_{\mathrm{vessel}}=
Y_{\mathrm{artery}}\lor
Y_{\mathrm{vein}}\lor
Y_{\mathrm{crossing}}\lor
Y_{\mathrm{uncertain}}.
$$

“类别不明”是动静脉类别不明，不应默认映射为背景。实施前必须列出实际 RGB 唯一值、像素计数、异常颜色与边界值；若存在压缩/抗锯齿颜色，先建立明确容差或查作者代码，不能简单使用“所有非黑像素”而不审计。

### 7.4 外部评测协议

1. 在 FIVES 上完成模型、checkpoint、阈值、预处理和滑窗规则冻结。
2. 外部集不参与训练、超参数、阈值、数据选择或方法去留。
3. 对主外部集完整 100 图报告；不得用模型成绩选择子集。
4. 使用原文件尺度滑窗推理，按分辨率、疾病和提供的质量字段分层；不同数据集的像素粗细阈值不作物理比较。
5. 若以后对外部集做适配，另建开发/测试隔离，不能覆盖本次零适配结果。
6. 质量匹配若开展，只能用预先定义、与模型输出无关的图像/元数据规则；完整外部集结果必须保留，并报告删去多少样本。

### 7.5 外部不可得时的降级

- Fundus-AVSeg 文件不可用：切换 HRF 全集，并把结论写为小样本、有域变化的外部测试。
- HRF 也不可用：可用 CHASE_DB1 压力测试，按儿童聚类；观察者 1 作为预先固定主参考，观察者 2 做敏感性分析。
- 所有外部数据均不可得：完成内部冻结实验、外部数据访问记录与可运行外部适配脚本，不虚构外部性能；课程项目仍可完成。

---

## 8. R6：最小实验表与正负结果路径

### 8.1 正式实验组

| 组 | 网络与损失 | 回答的问题 | 正式重复 | 是否主线 |
| --- | --- | --- | --- | --- |
| M0 | 固定 U-Net + $\mathcal{L}_{\mathrm{pix}}$ | 普通像素基线能达到什么水平 | 3 种子 | 是 |
| M1 | M0 + 普通 $\mathcal{L}_{\mathrm{SRec}}$ | 加一个明确的普通骨架召回项是否有增量价值 | 3 种子 | 是，背景对照 |
| M2 | M0 + $\mathcal{L}_{\mathrm{WSRec}}$ | 粗细权重相对普通骨架召回是否有额外价值 | 3 种子 | **是，主比较** |
| M3 | M2 + 图像级质量标量 | 经审计后，质量加权是否值得探索 | 0 或 3 种子 | 否，条件式探索 |

主要对照差值：

$$
\Delta_{\mathrm{main}}=M2-M1.
$$

次要差值：

$$
\Delta_{\mathrm{skeleton}}=M1-M0.
$$

### 8.2 公平性冻结项

所有正式组固定：

- 完整图像划分与裁块来源；
- 网络结构、通道数、初始化方案；
- 512 或 768 裁块尺寸；
- 图像归一化、增强、FOV 采样；
- 优化器、学习率计划、最大优化步数、有效 batch；
- 验证频率、checkpoint 选择、二值化阈值规则；
- 滑窗重叠、融合、padding；
- 每个种子的裁块/增强随机流；
- 日志、失败记录和结果计算代码。

M1 与 M2 共用 $\lambda_s$；M2 只增加预先定义的权重图。若 M2 获得更多调参机会，必须给 M1 同等预算或将额外尝试标为探索。

### 8.3 短跑与完整训练

短跑只用于：

- 验证数据链、loss 有限、梯度方向、显存和吞吐；
- 选择一次 $\lambda_s$ 或决定 32/16 基础通道；
- 识别明显不收敛或 Precision 崩溃。

短跑曲线未收敛时，不根据微小指标差宣布胜负。完成 G2 后冻结正式训练步数。候选最多允许**一次有明确问题指向的修订**，例如权重上限过大导致 Precision 崩溃；修订后重新运行受影响的完整组并保留原失败记录。不允许持续堆模块寻找正结果。

### 8.4 正负结果路径

| 结果 | 行动 | 报告口径 |
| --- | --- | --- |
| M2 稳定改善且 Precision 合格 | 完成外部冻结测试；不再增加模块 | 支持本协议下的粗细权重增量价值 |
| M2 无明显提升，M1 有效 | 停止修改 M2；以 M0–M1 为可靠主结果 | 粗细加权无额外价值，普通骨架项仍可讨论 |
| M1、M2 均无明显提升 | 完成公平负结果、案例和外部评测；不停止课程项目 | 在本网络/FIVES/预算下骨架项未显示实践增益 |
| 主指标升、Precision 超容限 | 可进行一次降低 $a_{\max}$ 或 $\lambda_s$ 的依据明确修订；仍失败则停止 | 召回收益的误检代价不可接受 |
| 种子方向冲突 | 检查日志、阈值与数值；无错误则报告不稳定 | 不能选最好种子 |
| 内部有效、外部不支持 | 检查格式/FOV；无错误则保持结果 | 方法可能依赖 FIVES 域，外部泛化证据不足 |
| 数据错位、指标错误、拼接错误 | 修复并重跑；不能作为负结果 | 这是实施失败，不是方法无效证据 |
| 外部文件不可得 | 使用备选或降级为内部实验 | 明确访问限制，不虚构外部结论 |

---

## 9. R7：G0–G5、资源与后台推进

### 9.1 修订后的关卡

| 门 | 输入证据 | 可检查的通过条件 | 未通过行动 | 可继续的独立任务 | 必须人工裁决 |
| --- | --- | --- | --- | --- | --- |
| G0 问题与边界 | 本报告、课程要求 | 一句话主问题、主指标/护栏、负结果可交付、无真实连续性/临床强主张 | 缩小问题，不停止项目 | 数据下载、仓库锁定 | 三人确认研究问题和课程成功标准 |
| G1a 训练数据 | FIVES 压缩包与 manifest | 计数/配对/尺寸/值域/许可/划分可复核；无阻塞串位 | 修复或换数据版本；严重不可用才更换主数据 | 方法单元测试 | 接受图像级限制或决定换数据 |
| G1b 质量分支 | 质量表交叉分布与盲审 | 足量低质量、疾病不严重混杂、标签仍可用 | 删除训练权重，保留分层 | M0–M2 全部工作 | M3 是否启动 |
| G1c 外部证据 | Fundus-AVSeg/HRF 文件审计 | 图像标签可配对、许可明确、二值化/FOV规则冻结 | 切换备选或取消外部实跑 | 内部训练与报告模板 | 主外部角色与限制 |
| G2 流程与测量 | 数据单元测试、M0 试跑、指标合成测试、资源 profile | 可重建一次基线；滑窗无缝；指标反应符合预期；512 运行稳定；记录显存与时间 | 修复管线或降通道/裁块；不进入正式比较 | 文献、外部清单 | 网络宽度、训练步数、阈值规则、实践阈值冻结 |
| G3 候选去留 | M1/M2 开发短跑、错误审计 | 继续原候选、允许一次依据明确修订，或停止修改 | 退回 M0–M1，不停止课程项目 | 报告、复现脚本、外部格式审计 | 是否使用唯一修订机会 |
| G4 结论强度 | 9 次正式训练、内部测试、外部结果、案例 | 结果完整，主比较预注册，限制与不确定性可说明 | 降低结论，不隐藏负结果 | 复现和演示 | 正/负/不确定口径 |
| G5 扩展 | 主线、复现、报告已完成且有余量 | 扩展回答明确问题，不威胁收尾 | 不做扩展 | 演示优化 | 第二外部集、形态测量或质量 M3 是否值得 |

### 9.2 少量后台任务包

| 包 | 目标 | 输入 | 产物 | 验收 | 依赖 | 重试/修订上限 | 交接点 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P1 数据审计 | 形成可训练且可追溯的 FIVES/外部 manifest | 原始压缩包、论文 | manifest、哈希、异常表、质量交叉表、样例叠加 | B 人工抽查，A/C 各复核若干样例 | 下载成功 | 脚本修订 2 次；数据源更换需人工 | G1 |
| P2 损失与指标 | 实现 M0–M2、主指标、clDice、单元测试 | 锁定公式、合成图 | 代码、测试报告、梯度/空值报告 | A/B 交叉复核，全部合成案例通过 | 无需完整数据可先做 | 2 次修订 | G2 |
| P3 基线与资源 | 得到可信 M0 和设备实测 | FIVES、固定配置 | 显存/吞吐表、一次可重建基线、验证曲线 | 配置、checkpoint、逐图指标可重建 | P1/P2 | 资源降级 1 次 | G2 |
| P4 候选筛选与冻结 | 决定 M1/M2 是否正式运行 | M0、候选实现 | 短跑包、盲审图、G3 决断记录 | 不用测试集；问题与行动对应 | P3 | 候选修订最多 1 次 | G3 |
| P5 正式实验与外部 | 运行 9 次、冻结评测和外部测试 | 冻结配置 | 每运行结果包、内部/外部逐图表、统计摘要 | B 重算指标，C 追溯配置与来源 | G3/G1c | 基础设施失败可同配置重跑 1 次 | G4 |
| P6 复现与交付 | 让非主开发者重建主要结果 | 清洁环境、锁定产物 | 环境锁、推理命令、复现日志、报告和演示 | 至少一名非主开发组员独立复现主表/样例 | P5 | 文档修订 2 次 | 课程提交 |

后台执行可以生成产物、跑训练和汇总日志，但不等于组员理解。A、B、C 均需能解释主问题、损失差别、指标边界和至少一例失败。

### 9.3 人工、后台、GPU 与等待分别估计

**主动人工检查点**建议控制在 8 类，而不是沿用 200–300 人时总表：

1. G0 问题签字；
2. 数据/许可/划分审计；
3. 10–20 组叠加样例复核；
4. 指标合成测试与公式复核；
5. 设备 profile 与基线复核；
6. G3 候选去留；
7. 正式结果、案例和统计解释；
8. 独立复现与演示。

**后台核心产物**约为：1 份数据 manifest、1 份异常表、1 份指标测试报告、1 份设备 profile、9 个正式运行包、1 个内部逐图结果包、1 个外部结果包、1 个复现包。数量用于控制交接，不代表后台永不失败。

设各组一次完整训练的实测 GPU 时间分别为 $T_0,T_1,T_2,T_3$。最小正式 GPU 占用为：

$$
G_{\mathrm{formal}}=3T_0+3T_1+3T_2.
$$

若质量分支通过并增加 M3：

$$
G_{\mathrm{formal+quality}}=G_{\mathrm{formal}}+3T_3.
$$

短跑、资源 profile、失败重跑、推理与外部评测另计。不得假设 $T_0=T_1=T_2$，也不得根据显卡型号预先承诺速度。

**外部等待**包括数据下载、远程主机可用性和可能的课程答复。它们可以与 P2 指标/损失开发并行，不应阻塞所有工作。

### 9.4 设备与裁块裁决

- 512×512 为学校 8 GB 设备的**初始默认**，不是已验证可行。
- 初始可 profile 普通 U-Net 通道 32–64–128–256–512、AMP、batch 1；若 OOM 或吞吐不可接受，只允许在正式前统一降为 16–32–64–128–256，所有组保持一致。
- 768×768 只有在峰值显存、有效 batch、单步时间和整图收益均实测可接受时才进入；不得将学校 512/窄模型与家中 768/宽模型混入同一正式比较。
- 可用梯度累积固定有效 batch；所有组必须使用相同有效 batch 和优化步数。
- 家中 RTX 5080 可用性是依赖，不是前提。主线应至少能在学校设备的降级配置上完成。

### 9.5 与 10 周窗口相容的相对阶段

不虚构具体日期，按依赖组织：

1. **入口与审计阶段**：G0、G1，数据、许可、manifest、患者/质量/外部状态。
2. **可信流程阶段**：P2/P3，指标、滑窗、M0、设备实测，完成 G2。
3. **候选筛选阶段**：M1/M2 短跑、唯一修订机会，完成 G3。
4. **正式冻结阶段**：9 次完整训练、内部测试、外部零适配评测。
5. **解释与复现阶段**：统计、案例、负结果、非主开发者复现、报告与演示。

必须在进入最后一个阶段前停止增加候选；扩展只在主要结果、恢复脚本和报告骨架均已完成后启动。

---

## 10. R8：旧大纲的保留、修改、降级与删除

| 旧要素 | 裁决 | 修订内容与依据 |
| --- | --- | --- |
| 课程成功标准 | **保留并强化** | 成功是可信、公平、可解释、可复现的实验；候选失败可交付 |
| 必须证明创新或显著提升 | **删除** | 与课程定位冲突；已有 cbDice 与宽度权重先行工作 |
| FIVES 为主开发数据 | **保留但条件化** | 先过文件/划分/质量审计；患者映射缺失不阻塞图像级实验 |
| 原尺度裁块、整图拼回评价 | **保留并澄清** | “原尺度”只指公开文件像素尺度，不是物理尺度 |
| 普通 U-Net 与像素基线 | **保留** | 适合课程资源与公平对照 |
| “普通骨架损失” | **修改** | 明确为 Skeleton Recall Loss，固定论文、实现和公式 |
| 主问题为真实结构连续性恢复 | **修改** | 改为标签支持的细血管骨架召回；FIVES 不为连续性补画 |
| 总体 clDice 为唯一主指标 | **修改** | 主指标改为细血管骨架召回；clDice 为必要次要，Precision 为护栏 |
| 粗细权重 | **保留为主候选** | 用全图标签 EDT 与中心线半径，限幅，主要比较 M2−M1 |
| 质量权重必做 | **降为可选** | 质量是整图评分且分布/混杂未实测；先做质量分层 |
| 四组×三种子固定 12 次 | **修改** | 最小正式为 M0–M2 共 9 次；M3 仅条件式增加 3 次 |
| 累计四组证明协同 | **删除** | 无质量-only 组不能识别交互作用 |
| CHASE_DB1 默认主外部 | **修改** | Fundus-AVSeg 为主域变化集，HRF 备选，CHASE 为压力测试 |
| 外部集必须“条件接近” | **删除** | 当前没有足够证据证明匹配；如实报告域变化 |
| 第二外部集 | **降为可选** | 仅主线完成且第一外部结果需要解释时启动 |
| 形态测量误差传播 | **降为可选** | 不直接回答主问题，需额外验证参考和统计 |
| 失败预警系统 | **降为可选** | 只有基础分割/复现完成后，且能定义明确失败标签时启动 |
| 旧 200–300 人时决定 10 周可行性 | **删除** | 改为任务包、人工检查点、实测 $T_m$ 和 GPU 公式 |
| G3 不通过即停止项目 | **删除** | G3 只决定候选是否继续修改；失败后完成 M0–M1 与负结果 |
| 测试集隔离、匹配预算、失败留痕 | **保留** | 是有效正/负结果的核心 |

### 10.1 修订版执行方案

1. 用 FIVES 官方 600/200 作为暂定图像级外框，从 600 中冻结 480/120 开发划分；患者字段若获得则升级为患者分组。
2. 完成 FOV、滑窗和指标合成测试后，训练 M0 基线并实测资源。
3. 实现普通 Skeleton Recall 作为 M1；以同一 $\lambda_s$ 实现粗细加权 M2。
4. 只用开发集决定一次候选参数与 Precision 护栏；正式运行 M0–M2 三种子。
5. 在 FIVES 200 图上进行一次冻结内部测试；随后在 Fundus-AVSeg 全集进行零适配外部域变化测试。
6. 无论 M2 正、负或不稳定，都完成逐图结果、案例、限制、恢复脚本和一名非主开发组员复现。
7. 质量 M3、第二外部集和形态测量只在 G5 讨论。

---

## 11. 决断清单 D01–D12

| ID | 问题 | 已核实证据 | 尚缺证据 | 推荐 | 替代与失败路线 | 时点分类 |
| --- | --- | --- | --- | --- | --- | --- |
| D01 | 唯一主问题 | FIVES 标签和细血管可计算 | 无物理尺度/真实连续性真值 | 标签支持的细血管骨架召回 + Precision | M0–M1 适用性复现 | **训练前必须解决** |
| D02 | 骨架对照 | Skeleton Recall 定义/代码/许可明确 | 普通 U-Net 本地运行 | 选 SRL | soft-clDice 仅可选参考 | **训练前必须解决** |
| D03 | 粗细权重 | EDT/骨架先行方法存在 | FIVES 实际半径分布 | 全图中心线半径、0.5–2 限幅 | 权重全 1，退回 M1 | **训练前必须解决** |
| D04 | FIVES 文件 | 论文/数据页可访问 | 字节与逐项审计 | 下载并建 manifest | 数据不可用才换主集 | **训练前必须解决** |
| D05 | 患者映射 | 800 图/573 人 | 公开 ID/眼别 | 能得则分组；不能得则图像级限制 | 外部 100 患者集补充 | **正式测试前冻结** |
| D06 | 质量权重 | 三项整图二值质量；疾病相关线索 | 开发交叉分布与标签盲审 | 从主线删除 | 通过审计后 M3 | **可带限制继续** |
| D07 | 指标 | clDice/SRL 定义可查 | 实际 $\tau$、指标实现 | 主细骨架召回、Precision 护栏 | 失败则只用 Dice/clDice 并缩小主张 | **正式对照前必须冻结** |
| D08 | 主外部 | Fundus 100 患者/五色/CC BY | 实际像素/FOV | Fundus 主、HRF 备、CHASE 压力 | 无外部则诚实降级 | **正式外测前冻结** |
| D09 | 主比较与阈值 | 匹配设计可行 | 基线波动 | M2−M1；验证阈值规则 | 无数值前不虚构门槛 | **正式测试前必须冻结** |
| D10 | 修订预算 | 课程允许失败 | 首次短跑问题 | 候选最多一次依据明确修订 | 停止修改，完成负结果 | **筛选启动前冻结** |
| D11 | 后台交接 | 三人职责明确 | 实际 Goal 吞吐/失败率 | P1–P6 产物式交接 | 人工复核保底 | **批量执行前冻结** |
| D12 | 收尾与扩展 | 10 周、需复现报告 | 精确截止/评分细则 | 主线完成前不扩展 | G5 才启动可选项 | **正式实验前建立** |

---

## 12. 首先核验的事项与优先级

### P0：立即阻塞项

1. 下载 FIVES，记录版本、许可、SHA-256、目录、800 对配对和唯一像素值。
2. 检查 `Quality Assessment.xlsx` 的 800 行映射、质量位型×疾病分布，以及是否存在任何患者/眼别线索。
3. 建立固定 FOV 规则；验证全图标签、骨架、EDT 和滑窗坐标。
4. 用合成图通过主指标、Precision、clDice、空骨架和接缝测试。
5. 在学校设备实测 512 裁块的峰值显存、单步时间、推理拼接时间；再决定网络宽度和是否测试 768。

### P1：不阻塞内部管线但阻塞外部主张

1. 下载并审计 Fundus-AVSeg v2，列出五色实际值、文件配对、`eye ID`、两类分辨率和 FOV。
2. 若 Fundus 获取失败，核验 HRF 整包；仍失败再用 CHASE_DB1。
3. 获取课程评分细则，将额外硬性要求加入 G4/G5，不重写已冻结主比较。

### P2：可以带限制继续

- HRF 相机/视野冲突；
- 真实物理管径；
- 复杂质量匹配；
- 第二外部集；
- 形态测量和临床解释。

---

## 13. 来源索引、版本与检索记录

### 13.1 数据集原始来源

1. Jin K, Huang X, Zhou J, et al. [FIVES: A Fundus Image Dataset for Artificial Intelligence based Vessel Segmentation](https://www.nature.com/articles/s41597-022-01564-3). *Scientific Data*, 2022. 访问：2026-09-04。
2. [FIVES Figshare 数据记录](https://doi.org/10.6084/m9.figshare.19688169). 页面显示约 1.64 GB、CC BY 4.0；本轮未完成下载。访问：2026-09-04。
3. Deng Z, et al. [A Fundus Image Dataset for AI-based Artery-Vein Vessel Segmentation](https://www.nature.com/articles/s41597-025-05381-2). *Scientific Data*, 2025. 访问：2026-09-04。
4. [Fundus-AVSeg Figshare v2](https://doi.org/10.6084/m9.figshare.27938034). v2 日期 2025-04-30，约 202.91 MB、CC BY 4.0；本轮未完成下载。访问：2026-09-04。
5. FAU Pattern Recognition Lab. [High-Resolution Fundus Image Database](https://www5.cs.fau.de/research/data/fundus-images/). 访问：2026-09-04。
6. Kingston University. [CHASE_DB1 retinal vessel reference dataset](https://researchinnovation.kingston.ac.uk/en/datasets/chasedb1-retinal-vessel-reference-dataset-4/). 访问：2026-09-04。
7. Holm S, Russell G, Nourrit V, et al. [DR HAGIS—a fundus image database for the automatic extraction of retinal surface vessels from diabetic patients](https://pmc.ncbi.nlm.nih.gov/articles/PMC5299858/). *Journal of Medical Imaging*, 2017. 访问：2026-09-04。

### 13.2 方法与实现

1. Shit S, Paetzold JC, et al. [clDice—a Novel Topology-Preserving Loss Function for Tubular Structure Segmentation](https://openaccess.thecvf.com/content/CVPR2021/html/Shit_clDice_-_A_Novel_Topology-Preserving_Loss_Function_for_Tubular_Structure_CVPR_2021_paper.html). CVPR 2021；[作者代码，MIT](https://github.com/jocpae/clDice)。
2. Kirchhoff Y, Rokuss MR, et al. [Skeleton Recall Loss for Connectivity Conserving and Resource Efficient Segmentation of Thin Tubular Structures](https://arxiv.org/abs/2404.03010). ECCV 2024；[作者代码，Apache-2.0](https://github.com/MIC-DKFZ/Skeleton-Recall)。
3. Arora D, Kumar N, Gupta S. [Does the Skeleton-Recall Loss Really Work?](https://arxiv.org/abs/2508.11374). arXiv 预印本，2025。作为反证与实现风险来源，不视为定论。
4. Shi P, Hu J, et al. [Centerline Boundary Dice Loss for Vascular Segmentation](https://arxiv.org/abs/2407.01517). MICCAI 2024；[作者代码，Apache-2.0](https://github.com/PengchengShi1220/cbDice)。
5. Linaris JPM. [retinal-thin-vessels 软件包与宽度权重说明](https://pypi.org/project/retinal-thin-vessels/). 访问：2026-09-04；较新工作坊/软件证据，结论谨慎使用。
6. MONAI. [Sliding-window inference documentation](https://docs.monai.io/en/stable/inferers.html). 实施时应锁定实际安装版本；本报告仅采用 overlap、constant/gaussian blend 与 padding 的一般接口概念。
7. Mongan J, Moy L, Kahn CE. [CLAIM 2024 Update](https://pmc.ncbi.nlm.nih.gov/articles/PMC11304031/). 用于参考数据划分、参考标准和透明报告，不将完整临床规范机械设为课程准入门槛。

### 13.3 检索记录与限制

- 检索日期：2026-09-04。
- 重点关键词：`retinal vessel thin vessel loss`、`Skeleton Recall Loss`、`clDice`、`cbDice`、`vessel width weight mask`、`FIVES quality assessment`、`Fundus-AVSeg`、`HRF fundus dataset`、`CHASE_DB1`、`DR HAGIS`、`sliding window inference`。
- 优先纳入原始论文、官方数据页和作者仓库；二手综述仅用于发现线索，不作为关键数值的首要证据。
- 未执行穷尽数据库检索、正式 PRISMA 流程、作者联系、账号申请或受限资源绕过。
- 未下载成功的数据压缩包均标为“入口可见、文件未实测”；仓库均标为“公开可访问、未在本地运行”。

---

## 14. 最终裁决

**课程项目整体 GO。**最稳健的主线不是承诺创造新的拓扑损失，而是在统一 U-Net 与训练协议下，回答一个窄而可验证的问题：有限粗细权重相对于普通 Skeleton Recall 是否提供稳定的细血管标签骨架召回增益，以及该增益是否值得其误检代价。

质量标签的价值先体现在**分层评价和解释**，不是自动进入训练损失。Fundus-AVSeg 提供最有价值的外部来源，但应明确定位为域变化测试。即使 M2 没有提升，只要数据、指标、对照、负结果、外部限制和复现链完整，项目仍达到课程级可信交付。
