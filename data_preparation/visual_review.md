# 数据准备目视核验

日期：2026-09-04。检查者：本任务助手，通过本地图像查看工具检查已生成的两张对照图。

结论：**本阶段目视检查通过。** 结论限定于检查的样例和空间转换，不评价原始血管标注的临床正确性。

## 裁块与增强

检查 `paired_crops.png` 的四行：127_A、259_D、440_G、532_N。每行包括整图裁块位置、增强后 RGB、二值标注、青色标注叠加。

- 原图裁块位置与增强后区域相符，主血管走向和分叉能对应。
- 水平/垂直翻转、90/270 度旋转后的图像和标注方向一致，没有观察到整体位移或单侧翻转。
- 标注显示为二值血管区域；其细节和宽度沿用原标签，没有在加载过程中插值。
- 图中保留原始照明差异、病变外观、黑边和边界缺口。部分原图黑边角落存在原始文字；后续 FOV/边界方案需要考虑这些内容，本次未去除。

## 异常用例

检查 `edge_cases.png`：447_G、448_G 和一张 RGBA 标注示例 107_A。

- 两张空标注图的原图非常暗，标签为全黑；叠加结果保持原图，没有伪造血管标签。
- 107_A 的 RGBA 标签在确认不透明和 RGB 通道一致后解码，叠加位置与血管走向相符。
- 5 组重复图的 10 份标注由程序逐组核对：原图像素完全相同，标注差异分别为 10,594、13,989、76,872、20,736、26,473 个像素。没有合并或丢弃标注。

## 复核范围与证据

目视检查是样例抽查；全量读取和转换正确性由 `verification.json`、`unit-tests.log` 与 `draw_signatures.json` 补充。两张已查看图片的 SHA-256 记录在 `verification.json` 的 `visual_review.image_sha256` 中。

本次包含 600 对原始训练图的读取核对、16 种几何组合测试、128 个抽样的单/双进程一致性检查。尚未训练模型，也未检查整图预测拼接。
