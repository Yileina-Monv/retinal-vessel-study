# M0 工程试跑报告

日期：2026-09-04。运行编号：`m0_pilot_2026-09-04_01`。

**GO：可以进入共同基线配置裁决与更长的试跑。** 本次已完成真实数据训练、日志、完整检查点、独立进程续训和整图预测。模型开始学到血管结构，当前设备能够承担这一配置。正式基线、收敛预算和增量研究效果尚未确定。

## 本次实际完成的范围

沿用已核验的数据清单：FIVES 原始训练分区内的 32 张试训图和 8 张开发验证图。两组按已知同图组隔离；没有患者标识，不能据此声称患者独立。原始 200 张测试图、Fundus-AVSeg 和 HRF 本次均未使用。

模型为 776 万参数的 2D U-Net，采用 BCE + soft Dice、原尺度 512×512 裁块、FP16 混合精度。微批量 2、梯度累积 2 次，有效批量 4。主线完成连续 256 次参数更新、1,024 次裁块抽样；另有 4 次专用于续训核对的参考更新。最终结果使用第 256 步检查点，没有按验证成绩挑选模型。

本次损失与指标覆盖全部图像像素，**没有使用眼底有效视野（FOV）掩膜**；输入仅除以 255，二值阈值固定为 0.5。质量字段仅用于既有小样本覆盖，不参与损失。这些配置在运行前写入[试跑协议](试跑协议_v1.md)。

## 可学习性与开发集结果

| 检查对象 | 初始化 | 第 256 步 |
| --- | ---: | ---: |
| 4 个固定训练裁块：平均损失 | 1.5248 | 0.7024 |
| 4 个固定训练裁块：平均 Dice | 0.1358 | 0.6007 |
| 8 张开发图：图像宏平均 Dice | 0.1345 | 0.6559 |
| 8 张开发图：图像宏平均 Precision | — | 0.6518 |
| 8 张开发图：图像宏平均 Recall | — | 0.6805 |

训练诊断裁块属于训练图，只用于检查拟合。开发指标是逐图计算后求均值，不能与汇总所有像素的指标混用。32/8 的小样本经过疾病和质量覆盖选择，也不能视作完整数据分布的代表。

固定裁块 Dice 在第 32 步一度降到 0，随后上升，并在中途波动。本次支持“模型可学习”，不支持“已收敛”或“性能稳定”。全部 256 次主线更新的损失和梯度有限，没有 AMP 跳步。237 步触发梯度裁剪（约 92.6%），后续应结合更长曲线检查学习率与裁剪设置，不能仅凭这一比例判定失败。抽到的 3 个全背景裁块均按预定规则保留。

| 开发图（均来自原始 train 分区） | 最终 Dice |
| --- | ---: |
| 138_A | 0.5959 |
| 177_D | 0.8042 |
| 222_D | 0.2648 |
| 421_G | 0.8045 |
| 442_G | 0.4969 |
| 453_N | 0.7067 |
| 489_N | 0.8452 |
| 8_A | 0.7287 |

个体表现差异较大。这一轮的分数不作为正式基线成绩、跨数据集泛化证据或临床效果结论。

[学习曲线](../runs/m0_pilot_2026-09-04_01/figures/learning_curves.png)保留了训练波动和续训位置。完整指标见[结果摘要](summary.json)及[逐图评价](../runs/m0_pilot_2026-09-04_01/validation_final.json)。

## 独立进程续训与输出验收

第一进程在第 128 步保存完整状态，再运行 129–132 步作为不中断参考；第二进程重新加载第 128 步检查点，重做相同四步后继续到第 256 步。

- 两条路径的样本与裁块几何完全一致，最大损失差为 0。
- 模型、优化器、学习率调度器、AMP scaler、随机状态及数据游标的摘要完全一致。
- 主训练日志为连续的 1–256 步；最终检查点重新读取为 step=256、next_global_draw=1024，其绑定的代码、配置和清单与当前文件一致。
- 8 张整图概率输出均为 2048×2048、float32、有限且处于 [0,1]；对应二值 PNG 与概率图按 0.5 阈值的结果完全一致。
- 5 项 M0 单元测试通过，覆盖损失与梯度、梯度累积归约、不规则尺寸拼回、空集合指标和检查点状态序列化。

验收记录：[恢复比较](../runs/m0_pilot_2026-09-04_01/resume_check.json)、[输出核验与文件指纹](artifact_check.json)、[测试记录](unit-tests.log)。最终检查点为 [last.pt](../runs/m0_pilot_2026-09-04_01/last.pt)。这些核对证明本次配置下的工程一致性，不能替代不同设备、版本或完整研究的复现检验。

## 预测图目视检查

已查看学习曲线、4 个固定训练裁块的前后对照，以及按既定清单顺序每类取一张的 4 张开发图（138_A、177_D、421_G、453_N）；展示样本没有按得分优选。

主要血管网络已经可辨。仍可见细分支遗漏、局部断裂，以及眼底外缘、黑边角落文字、亮斑和纹理的误检。训练裁块中也有类似误检，说明这不是只出现在开发图上的问题。

所查看的整图缩略对照未见明显的大块错位或拼接空洞。合成数据测试已核对拼回坐标和权重机制，但本次目视检查不足以证明真实网络预测不存在接缝或上下文效应，也不能确认所有细血管和标注的临床正确性。

- [固定训练裁块对照](../runs/m0_pilot_2026-09-04_01/figures/fixed_training_examples.png)
- [开发图与错误叠加](../runs/m0_pilot_2026-09-04_01/figures/development_examples.png)：青色为正确检出，品红为误检，黄色为漏检。

## 资源实测

设备为 RTX 5080 16 GB；本次记录驱动 616.64、PyTorch 2.12.1+cu130、CUDA 13.0。完整原始记录见[资源摘要](resource_profile.json)。

| 项目 | 实测 |
| --- | ---: |
| 两段进程内计时之和 | 86.74 秒 |
| 256 步主训练循环总时长 | 70.73 秒 |
| 其中取数等待总时长 | 49.24 秒 |
| GPU 区域 CUDA 事件计时总和 | 21.18 秒 |
| 去除前 16 步后的更新耗时中位数 | 0.177 秒/步 |
| 同一范围 GPU 区域耗时中位数 | 0.079 秒/步 |
| PyTorch 峰值已分配显存 | 1.63 GiB |
| PyTorch 峰值保留显存 | 2.41 GiB |
| 采样到的进程树 RSS 最大值 | 3.39 GiB |
| 最终 8 张整图评价，含加载 | 4.31 秒 |
| 其中滑窗推理（392 个窗口） | 3.27 秒 |

86.74 秒不包含计时开始前的 Python、模型和数据初始化，也不包含两段进程间隔或本轮编码排错时间。显存数值不含其他进程及全部驱动开销；进程树 RSS 为离散采样且可能重复计算共享页。GPU 事件时长不是 GPU 利用率或能耗测量。

**当前短跑的主要等待来自数据供给，显卡容量有余量。** 因此下一步存在可独立完成的编程工作：减少加载进程反复启动与重复解码，再做稳定吞吐量测量。本次时长不能直接推算正式训练需要多久，尤其尚不知道各方法的收敛预算。

## 下一步决断点

本次已经提供了作出下一阶段选择的证据，无需继续追加同样规模的工程短跑。建议保留当前输出，下一步统一确定：

1. **共同基线的像素范围与预处理。** 确定 FOV 生成、损失与评价使用范围、边界及角落文字处理、归一化规则。眼底边缘误检使这项选择具有实际依据；目前不能预先宣称 FOV 一定改善分数。
2. **更长试跑的数据与预算。** 明确从当前 32/8 子集扩展的开发划分和训练长度，并先处理数据等待。沿用同图组隔离及已记录的异常标注规则；患者独立性仍未得到验证。
3. **比较入口。** 在上述共同条件确定后开展 M0/M1 的单种子较长试跑，再据学习曲线、资源及差异决定是否进入增量 A。B/C 仍可作为独立增量，当前结果不要求改动整体研究路线。

本次授权的一轮 M0 已完成，尚未启动 M1、更长训练或多种子正式实验。
