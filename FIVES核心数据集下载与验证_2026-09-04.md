# FIVES 核心数据集：已下载，800 对全量审计完成

核验日期：2026-09-04。全量审计及重复标注核对完成时间：北京时间 18:57。

**FIVES 是本项目的核心开发数据集。此前完成的 Fundus-AVSeg 和 HRF 是外部候选数据；本次已经补齐 FIVES。**

结论：**GO，进入本地读取与最小管线建设。** 已下载完整镜像并验证 800 对原图—标注、800 行质量评分。文件完整性通过；实际副本中存在 2 张空标注和 6 组同图不同标注，需要在开工时明确处理方式。本次未训练模型，也未修改任何原始图像、标注或评分。

## 1. 位置与来源

- 解压数据：[data_checks/fives_download_2026-09-04/datasets/FIVES](data_checks/fives_download_2026-09-04/datasets/FIVES)
- 压缩包：[fives_kaggle_v6.zip](data_checks/fives_download_2026-09-04/archives/fives_kaggle_v6.zip)
- 来源归属：[FIVES 原始论文](https://pmc.ncbi.nlm.nih.gov/articles/PMC9352679/)、[Figshare DOI](https://doi.org/10.6084/m9.figshare.19688169)。
- 实际下载：[Kaggle 镜像](https://www.kaggle.com/datasets/nikitamanaenkov/fundus-image-dataset-for-vessel-segmentation)，固定镜像版本 6，平台许可字段为 CC BY 4.0。
- 交叉核对：[另一份 Kaggle 镜像](https://www.kaggle.com/datasets/sushanthreddypotu/fives-dataset)，固定镜像版本 2。

原站 API 返回 HTTP 403、`Server: awselb/2.0`，与此前 Figshare 的访问问题一致。下载沿用现有代理，没有变更节点、分流或认证设置。大文件从已下载的 1,598,029,824 字节处续传成功，最终完整文件 MD5 与存储服务的 ETag 一致。

压缩包大小为 **1,763,102,639 字节，约 1.76 GB / 1.64 GiB**。SHA-256：

```text
ac15665bf20c6c6c7943dba707ca942f77ba3ca4a25cf785ba444cf7bbd191e0
```

### 来源核对能支持到哪里

两份镜像的 **1,600 个 PNG 的文件名、大小与 CRC32 全部匹配**；两份质量工作簿虽然文件字节不同，解码后的 **800 行疾病、图像编号和三项质量评分全部相同**。另一镜像只读取了约 592 KB 的 ZIP 索引及约 30 KB 的质量表，没有重复下载整个大包。

这是对副本一致性的交叉支持，不能代替与 Figshare 官方原包的密码学校验或逐文件对照。**官方原包同一性仍未证实；实验报告应保留镜像来源和版本记录。** 下列异常均指本次取得并交叉核对的副本，未据此断言官方原包必然存在同样问题。

## 2. 全量文件核验

| 项目 | 实测结果 |
|---|---|
| 原图与标注 | 800 对，全部配对、解码成功 |
| 文件分区 | train 600 对、test 200 对 |
| 疾病数量 | AMD、DR、青光眼、正常各 200 对；每类 train 150、test 50 |
| 尺寸 | 原图、标注全部为 2048×2048 |
| 原图编码 | 800 张 RGB |
| 标签编码 | 790 张 RGB、10 张 RGBA；RGB 三通道相同，RGBA 的透明通道均为 255 |
| 标签数值 | 去除已核实的通道冗余后，只含 0/255；原文件未改写 |
| ZIP 完整性 | 所有 1,602 个归档文件均完整读取并通过 CRC 检查 |
| 可读取数据目录 | 1,600 个 PNG + 1 个质量表，共 1,601 文件 |
| 附带系统缓存 | `train/Original/Thumbs.db` 保留在原 ZIP 中，未当成图像或提取至数据目录 |
| FOV 与患者映射 | 未发现单独 FOV 文件、患者 ID 或左右眼映射表 |
| 下载残留 | 没有未完成的 `.part` 文件；原始数据、压缩包已被 Git 忽略 |

数据表必须以 **`split/filename`** 标识样本。训练集和测试集有 50 个同名文件，仅凭文件名不能作为全局唯一键；这些同名不等于图像内容相同。

## 3. 质量表已取得并逐图对应

`Quality Assessment.xlsx` 的 Train/Test 两张表分别有 600/200 行，字段为 `Disease`、`Number`、`IC`、`Blur`、`LC`，全部连接到实际图像。按[原始论文的数据记录说明](https://pmc.ncbi.nlm.nih.gov/articles/PMC9352679/)，三项评分中的 1 表示该方面质量良好，0 表示较差；IC 对应照明与颜色失真，LC 对应低对比度。

| 分区 | 样本数 | IC 良好 | Blur 良好 | LC 良好 | 三项全部良好 | 至少一项较差 |
|---|---:|---:|---:|---:|---:|---:|
| train | 600 | 496 | 510 | 580 | 459 | 141 |
| test | 200 | 148 | 159 | 185 | 133 | 67 |
| 合计 | 800 | 644 | 669 | 765 | 592 | 208 |

质量分层所需字段已具备。是否把质量信息用于训练权重，仍是后续增量研究的选择，此次没有据这些分布选择权重、阈值或筛选子集。

## 4. 开工时需要处理的实测问题

### 两张空标注

`train/447_G.png`、`train/448_G.png` 的标注完全为零，两张图的三项质量评分也都是 0。图像本身可以解码，两个零标注在另一镜像中也有相同文件指纹。

原文件已保留，没有把它们自动删除或当作损坏文件。实施时需要明确空标签对骨架损失、召回率及统计分母的处理。

### 六组完全相同的原图，配有不同标注

800 个原图文件对应 **794 种不同的像素内容**。以下每组原图的文件字节和像素均完全相同，但组内血管标注不同；质量评分相同。

| 分区 | 重复原图 | 标注不一致像素数 |
|---|---|---:|
| train | 118_A、34_A | 10,594 |
| train | 121_A、61_A | 13,989 |
| train | 132_A、39_A | 76,872 |
| train | 134_A、55_A | 20,736 |
| train | 53_A、97_A | 26,473 |
| test | 32_A、48_A | 4,796 |

没有发现相同原图跨越当前的 train/test 分区。**以后从 600 张中再拆训练/验证集时，应按重复组一起分配，避免同图进入两侧。** 同图多标注如何保留、训练和汇总评价，也需要明确；此次没有任意挑选其中一个标签。

与已下载 HRF、Fundus-AVSeg 的原图文件进行 SHA-256 对照，没有发现字节完全相同的文件。这项检查不能排除重新编码后的重合，也不能证明跨数据集患者独立。

### 患者独立性仍无法从文件证明

论文报告 FIVES 来自 573 名受试者，但实际下载内容只有图像编号、疾病和质量字段，没有患者编号与眼别映射。794 种不同图像内容也不等于 794 名患者。当前能够明确的是图像和重复组级别，不能声称已经完成患者级分组验证。

## 5. 证据清单

- [下载记录](data_checks/fives_download_2026-09-04/download_evidence.json)
- [全量文件、空标签与重复标注审计](data_checks/fives_download_2026-09-04/format_evidence.json)
- [800 行样本清单](data_checks/fives_download_2026-09-04/fives_manifest.csv)
- [质量表审计与分层统计](data_checks/fives_download_2026-09-04/quality_evidence.json)
- [另一镜像的逐文件索引比较](data_checks/fives_download_2026-09-04/mirror_comparison_evidence.json)

项目的数据角色保持为：**FIVES 核心开发与内部评价；Fundus-AVSeg 外部候选；HRF 外部备用或补充。** 数据读取的准备已完成，具体实验协议在后续开工时确定。
